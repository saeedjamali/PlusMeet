# Changelog

تمام تغییرات قابل توجه در این پروژه در اینجا مستند می‌شود.

فرمت بر اساس [Keep a Changelog](https://keepachangelog.com/fa/1.0.0/) است.

## [نسخه در حال توسعه] - 2025-10-29

### ✨ ویژگی‌های جدید (New Features)

#### 🎨 جایگزینی آیکن‌ها و لوگوهای جدید

- ✅ **کامپوننت Logo هوشمند**: `src/components/ui/Logo.js`
  - پشتیبانی از 3 نوع: `horizontal`, `vertical`, `icon`
  - سوییچ خودکار بین light/dark mode
  - تنظیم ابعاد قابل تغییر
  - پشتیبانی از Link و priority loading
  - انیمیشن و drop-shadow زیبا
- ✅ **به‌روزرسانی Favicon و PWA**:
  - آیکن جدید در `src/app/layout.js`: `/icons/light-mode/icon/icon-10-01.svg`
  - بهبود `public/manifest.json` با آیکن‌های چندگانه (SVG + PNG)
  - پشتیبانی از `maskable` icons برای PWA
- ✅ **صفحه اصلی (`/`)**:
  - **Navigation**:
    - Desktop (>1024px): لوگو horizontal (120×35px) + menu items در nav bar
    - **Tablet & Mobile (≤1024px)**: Fixed Mobile Header جداگانه شامل:
      - لوگو icon (40×40px) در سمت راست
      - دکمه خروج قرمز (40×40px) با icon - فقط برای کاربران احراز هویت شده
      - همبرگر button طلایی بزرگ (50×50px) با animated icon
      - Border طلایی پایین header برای visibility بهتر
      - Shadow و hover effects
    - Mobile menu: Full-width dropdown از زیر header با slideDown animation
    - Desktop nav مخفی در موبایل
    - Theme-aware styling برای همه components
  - **Hero Section**: لوگو vertical واقعی (200×250px) دقیقاً در مرکز hero image بین floating cards
  - **Footer**: لوگو horizontal (120×35px) در footer
  - انیمیشن floatLogo + pulse زیبا، drop-shadow طلایی و hover effects
  - Z-index layering: Mobile Header (z-index: 1002) > Mobile Menu (z-index: 1000) > Mobile Overlay (z-index: 999) > Logo (z-index: 10) > Floating Cards (z-index: 5)
  - Responsive design: 
    - Desktop (>1024px): Hero main logo 200×250px, desktop navigation
    - Tablet (≤1024px): Hero main logo 160×200px, hamburger menu
    - Mobile (≤768px): Hero main logo 140×175px, hamburger menu
  - Theme-aware با shadow قوی‌تر در dark mode
  - Centered positioning با floatLogo animation
- ✅ **داشبورد یکپارچه (`/dashboard`)**:
  - **یک Dashboard برای همه**: همه کاربران به `/dashboard` دسترسی دارند
  - **RBAC کامل**: منوها و دسترسی‌ها بر اساس نقش کاربر نمایش داده می‌شود
  - **برای Admin/Moderator**:
    - Sidebar کامل با منوهای مدیریت
    - کاربران، نقش‌ها، تنظیمات، و...
    - دسترسی کامل به RBAC
  - **برای کاربران عادی**:
    - Sidebar با منوهای محدود
    - صفحه اصلی داشبورد با آمار و ارقام
    - دسترسی سریع به امکانات کاربری
  - **Desktop (>1024px)**:
    - Sidebar: لوگو horizontal وقتی باز است، icon وقتی بسته است
    - انیمیشن smooth برای تغییر اندازه
  - **Mobile & Tablet (≤1024px)**:
    - Fixed Mobile Header جداگانه شامل:
      - لوگو icon (36×36px) در سمت راست
      - عنوان "پنل مدیریت" در وسط
      - دکمه خروج قرمز (40×40px)
      - دکمه منو طلایی (50×50px)
    - Sidebar به صورت overlay از سمت راست
    - Sidebar Header مخفی در موبایل
    - Main Content با padding-top برای fixed header
- ✅ **صفحه لاگین (`/login`)**:
  - لوگو vertical (80×100px)
  - Drop shadow زیبا و انیمیشن‌های نرم
  - **یک Login برای همه**: همه کاربران به `/dashboard` هدایت می‌شوند
  - بعد از login، RBAC تعیین می‌کند کاربر چه منوهایی می‌بیند
- ✅ **Navigation**:
  - **دکمه داشبورد**: همه کاربران به `/dashboard` می‌روند
  - **در همه صفحات**: Desktop و Mobile navigation
- ✅ **آیکن‌های موجود**:
  - **Light mode**: horizontal (`logo-06-01.svg`), vertical (`logo-07-01.svg`), icon (`icon-10-01.svg`)
  - **Dark mode**: horizontal (`logo-09-01.svg`), vertical (`logo-08-01.svg`), icon (`favicon.svg`)

#### 📏 قابلیت Resize کردن Sidebar با Drag

- ✅ **Resizable Sidebar** (`src/app/dashboard/layout.js`):
  - اضافه شدن state برای `sidebarWidth` (پیش‌فرض: 280px)
  - state برای `isResizing` جهت tracking وضعیت drag
  - useEffect برای handle کردن mouse events (mousemove, mouseup)
  - محدودیت عرض: حداقل 200px، حداکثر 500px
  - **RTL Support**: محاسبه عرض با `window.innerWidth - e.clientX` برای layout راست‌چین
- ✅ **Resize Handle UI** (`src/app/dashboard/admin.module.css`):
  - نوار عمودی **12px** در لبه چپ sidebar (عرض بیشتر برای کلیک آسان‌تر)
  - نشانگر **3px×60px** در وسط با **opacity 0.3** (همیشه قابل رویت)
  - پس‌زمینه طلایی کم‌رنگ دائمی: `rgba(244, 163, 37, 0.05)`
  - hover effect: پس‌زمینه روشن‌تر + نشانگر primary color با opacity 1
  - **z-index: 1000** برای قرارگیری بالای همه عناصر
  - cursor: col-resize برای نشان دادن قابلیت resize
  - در موبایل (≤1024px) مخفی می‌شود
- ✅ **Dynamic Width**:
  - عرض sidebar با inline style تنظیم می‌شود: `width: ${sidebarWidth}px`
  - transition حذف شد برای resize روان‌تر
  - در sidebar بسته، عرض ثابت 80px با `!important`
  - `position: relative` به جای `sticky` برای پشتیبانی از resize handle
  - `flex-shrink: 0` برای جلوگیری از shrink شدن
- ✅ **UX بهتر**:
  - متن‌های بلند داخل sidebar دیگر قطع نمی‌شوند
  - کاربر می‌تواند عرض sidebar را بر اساس نیازش تنظیم کند
  - cursor به col-resize تغییر می‌کند
  - body.userSelect='none' در حین drag برای جلوگیری از selection
  - Debug logs موقت برای troubleshooting (console.log)

#### ⏱️ تنظیم زمان انقضای Token ها

- ✅ **Access Token Expiration** (`src/lib/middleware/auth.js`):
  - تغییر زمان انقضای access token از **7d** به **1h** (1 ساعت)
  - تغییر در function `generateToken`: `expiresIn = "1h"`
  - دلیل: امنیت بیشتر - token کوتاه‌مدت کمتر در معرض خطر است
- ✅ **Refresh Token Expiration** (`src/lib/middleware/auth.js`):
  - تغییر زمان انقضای refresh token از **30d** به **7d** (7 روز)
  - تغییر در function `generateRefreshToken`: `expiresIn: "7d"`
  - دلیل: متناسب با access token 1 ساعته، کاربر هر 7 روز یکبار دوباره login می‌کند
- ✅ **پیشنهاد**: برای تغییر راحت‌تر، می‌توانید این مقادیر را به متغیرهای محیطی منتقل کنید

#### 🎯 بهبود UX در صفحه Login و Dashboard Sidebar

- ✅ **Auto-Submit OTP** (`src/app/login/page.js`):
  - وقتی کاربر 5 رقم کد تایید را وارد می‌کند، فرم خودکار submit می‌شود
  - تاخیر 300ms برای UX بهتر (کاربر آخرین رقم را می‌بیند)
  - دیگر نیازی به کلیک دستی روی دکمه "تایید و ورود" نیست
- ✅ **Smart Sidebar Expansion** (`src/app/dashboard/layout.js`):
  - وقتی sidebar بسته است و کاربر روی منویی با زیرمنو کلیک می‌کند:
    - Sidebar خودکار باز می‌شود
    - بعد از 100ms، منو expand می‌شود تا زیرمنوها نمایش داده شوند
  - برای منوهای link-دار با زیرمنو:
    - اگر sidebar بسته است، کلیک اول sidebar را باز می‌کند (link اجرا نمی‌شود)
    - اگر sidebar باز است، کلیک به link می‌رود یا منو toggle می‌شود
  - تجربه کاربری روان‌تر برای دسترسی به زیرمنوها

#### 🎨 استایل‌های کامل و منظم Dashboard

- ✅ **فایل جدید**: `src/app/dashboard/dashboard.module.css` - استایل اختصاصی برای صفحه dashboard
- ✅ **Header Section**:
  - عنوان با gradient (primary → #ff6b6b)
  - Subtitle با رنگ secondary
  - دکمه Refresh با hover effects و shadow
- ✅ **Stats Grid**:
  - Responsive grid (auto-fit, minmax(250px, 1fr))
  - کارت‌های آمار با animation و hover effects
  - Color coding: `primary`, `accent`, `success`, `warning`
  - خط رنگی در بالا با animation scaleX
  - آیکن‌های gradient با border-radius
- ✅ **Quick Actions**:
  - Grid 2×2 برای دسترسی سریع
  - Hover effects با تغییر رنگ background
  - Animation translateY برای lift effect
  - آیکن‌ها با transition نرم
- ✅ **Recent Activities**:
  - لیست فعالیت‌های اخیر
  - آیکن‌های رنگی بر اساس نوع (user, event, report, profile)
  - Hover effect با translateX
  - Border و shadow نرم
- ✅ **Empty State** (برای کاربران عادی):
  - طراحی زیبا با border dashed
  - آیکن بزرگ با animation float
  - پیام واضح و دکمه بازگشت
  - Gradient button با hover effects
- ✅ **Responsive Design**:
  - **Desktop (>1024px)**: Grid 2 ستونه، همه عناصر کنار هم
  - **Tablet (≤1024px)**: Grid 1 ستونه، header عمودی، full-width buttons
  - **Mobile (≤768px)**: Stats grid 1 ستونه، quick actions عمودی، padding کمتر
  - **Mini Mobile (≤480px)**: Stat cards عمودی با text-align center
- ✅ **Dark Mode Support**:
  - رنگ‌های تطبیق‌یافته با theme
  - Background و border colors از CSS variables
  - Contrast بهتر در حالت تیره
- ✅ **Animations**:
  - `fadeIn`: برای ورود صفحه (0.4s)
  - `float`: برای آیکن empty state (3s infinite)
  - Hover transforms: translateY(-4px), translateX(-4px)
  - Transition‌های smooth برای همه عناصر (0.3s ease)

---

#### اصلاح و بهبود فیلترهای `/admin/users`

- ✅ **مشکل حل شد**: نمایش وضعیت کاربران بر اساس `user.state` به جای `user.status`
- ✅ **تغییرات Frontend**:
  - `statusFilter` → `stateFilter` در state و UI
  - `getStatusBadgeClass` → `getStateBadgeClass`
  - `getStatusPersianName` → `getStatePersianName`
  - اضافه شدن وضعیت `pending_verification` (در انتظار تایید) با رنگ قهوه‌ای
  - اضافه شدن CSS class `badgePendingVerification`
  - اضافه شدن `getUserTypePersianName` برای نمایش فارسی نوع کاربر
- ✅ **تغییرات Backend API**: 
  - `GET /api/admin/users`: query parameter `status` → `state`
  - فیلتر `query.status` → `query.state`
  - **فیلتر گروهی userType**: اگر `individual` انتخاب شود، `individual` و `individual_freelancer` رو شامل می‌شه. اگر `organization` انتخاب شود، همه `organization_*` subtypes رو شامل می‌شه.
  - لاگ activity با `state` به جای `status`
- ✅ **وضعیت‌های پشتیبانی شده**:
  - `active` (فعال) - 🟢 سبز
  - `verified` (تایید شده) - 🔵 آبی تیره
  - `pending_verification` (در انتظار تایید) - 🟤 قهوه‌ای
  - `pending` (در انتظار) - 🔵 آبی
  - `suspended` (معلق) - 🟡 زرد
  - `deleted` (حذف شده) - ⚫ خاکستری
  - `unregistered` (ثبت نام نشده) - 🔘 پیش‌فرض
- ✅ **فیلتر نوع کاربر عملیاتی**:
  - 👤 فردی (شخصی / آزاد): شامل `individual` و `individual_freelancer`
  - 🏢 سازمانی (شرکت / تیم / NGO): شامل همه `organization_*` subtypes
  - 🏛️ دولتی (ادارات / نهادها): فقط `government`
  - نمایش فارسی نوع کاربر در جدول با styling مخصوص

---

#### بهبود صفحه مدیریت نقش‌ها (`/admin/rbac/roles`)

- ✅ **نمایش تعداد واقعی کاربران**: تعداد کاربران هر نقش از دیتابیس خوانده می‌شود
  - API: `GET /api/admin/rbac/roles` حالا `userCount` هم برمی‌گرداند
  - شمارش با `User.countDocuments({ roles: role.slug })`
  - نمایش در کارت هر نقش: "👥 X کاربر"
- ✅ **امکان ویرایش اولویت (Priority)**:
  - فیلد جدید در صفحه ویرایش نقش: "اولویت (Priority)"
  - رنج: 0-100 (بالاتر = مهم‌تر)
  - راهنما: "نقش‌های با اولویت بالاتر در لیست‌ها و گزینه‌ها زودتر نمایش داده می‌شوند"
  - مثال: Admin: 100، Moderator: 60، User: 10
- ✅ **امکان فعال/غیرفعال کردن نقش (Toggle Switch)**:
  - **UI**: Toggle switch مدرن با انیمیشن
  - رنگ سبز (✅ فعال) / قرمز (❌ غیرفعال)
  - راهنما: "نقش‌های غیرفعال قابل استفاده نیستند و نمایش داده نمی‌شوند"
  - Default: فعال (`isActive: true`)
- ✅ **محدودیت‌های ویرایش**:
  - **نقش‌های سیستمی**: فقط `priority`, `isActive` و `permissions` قابل ویرایش است (نه نام، slug، و...)
  - **نقش‌های سفارشی**: همه فیلدها قابل ویرایش هستند
- ✅ **API**: `PUT /api/admin/rbac/roles/:id` با پشتیبانی کامل از `priority` و `isActive`
- ✅ **UI/UX**: Priority و Status در یک ردیف (2 ستون) با responsive design

---

#### صفحه ایجاد کاربر جدید (`/admin/users/create`)

- ✅ **مسیر**: `http://localhost:3000/admin/users/create`
- ✅ **قابلیت‌ها**:
  - فرم کامل با 5 تب (مشابه EditUserModal)
  - **اطلاعات پایه**: شماره موبایل*, نام*, نام خانوادگی\*, رمز عبور (اختیاری), نام نمایشی, ایمیل, نوع کاربر, وضعیت, کد ملی, نقش‌ها (multi-select), بیوگرافی
  - **اطلاعات شخصی**: تاریخ تولد, جنسیت, شهر, آدرس (فقط فردی)
  - **اطلاعات سازمانی**: نام سازمان, شماره ثبت, شناسه ملی, وب‌سایت, ایمیل سازمان, توضیحات (فقط سازمانی/دولتی)
  - **آدرس و تماس**: آدرس کامل, شخص تماس (فقط سازمانی/دولتی)
  - **شبکه‌های اجتماعی**: Instagram, Telegram, LinkedIn, Twitter/X, Facebook, Website
- ✅ **API**: `POST /api/admin/users` - ایجاد کاربر جدید
- ✅ **Validation**:
  - شماره موبایل: الزامی, فرمت `09XXXXXXXXX`, یکتا
  - نام و نام خانوادگی: الزامی
  - ایمیل: فرمت استاندارد
  - کد ملی: 10 رقمی
  - رمز عبور: اختیاری (اگر نباشد، کاربر با OTP لاگین می‌کند)
- ✅ **Password Hashing**: با `bcrypt` (10 salt rounds)
- ✅ **Activity Log**: ثبت `user.create` با اطلاعات کاربر جدید
- ✅ **RBAC**: فقط Admin می‌تواند کاربر جدید ایجاد کند
- ✅ **UI/UX**:
  - دکمه "+ ایجاد کاربر جدید" در صفحه `/admin/users`
  - نمایش داینامیک تب‌ها بر اساس نوع کاربری
  - نقش "کاربر عادی" همیشه انتخاب شده (disabled)
  - Character counter برای بیوگرافی (500 کاراکتر)
  - دکمه "بازگشت" برای رفتن به لیست کاربران
  - Responsive design

---

#### مدال ویرایش کامل کاربر در Admin Panel

- ✅ **کامپوننت**: `EditUserModal.js` + `EditUserModal.module.css`
- ✅ **قابلیت‌ها**:
  - **Tab ها**: اطلاعات پایه، اطلاعات شخصی (فردی), اطلاعات سازمانی (سازمان), آدرس و تماس، شبکه‌های اجتماعی
  - **اطلاعات پایه**: نام، نام خانوادگی، نام نمایشی، ایمیل، کد ملی، بیوگرافی، نوع کاربر، وضعیت
  - **فردی**: تاریخ تولد، جنسیت، شهر، آدرس
  - **سازمانی**: نام سازمان، شماره ثبت، شناسه ملی، وب‌سایت، ایمیل سازمان، توضیحات
  - **آدرس**: شهر، استان، کد پستی، آدرس کامل، شخص تماس (نام، سمت، تلفن، ایمیل)
  - **شبکه‌های اجتماعی**: Instagram, Telegram, LinkedIn, Twitter/X, Facebook, Website
  - **نمایش داینامیک**: Tab ها بر اساس `userType` تغییر می‌کنند
- ✅ **API**: `PUT /api/admin/users/:id` - ویرایش اطلاعات کاربر
  - `GET /api/admin/users/:id` - دریافت اطلاعات یک کاربر
  - `DELETE /api/admin/users/:id` - حذف کاربر
- ✅ **Validation**:
  - فرمت ایمیل
  - کد ملی 10 رقمی
  - Moderator نمی‌تواند Admin را ویرایش کند
  - Admin نمی‌تواند خودش را حذف کند
- ✅ **Activity Log**: ثبت `user_update`, `user.view`, `user.delete` با metadata کامل
- ✅ **UI/UX**:
  - طراحی مدرن با tab navigation
  - Responsive برای موبایل
  - Animation ها و transition های نرم
  - Character count برای textarea (500 کاراکتر بیوگرافی)
- ✅ **RBAC**: Moderator می‌تواند کاربران را ویرایش کند (به جز Admin)

---

### 🔄 تغییرات (Changes)

#### تبدیل منوها به سیستم کاملاً دینامیک (Database-Driven Menu System)

- ✅ **حذف شد**: تمام منوهای هارد کد از `layout.js`
- ✅ **اضافه شد**: سیستم منوهای کاملاً دینامیک:
  - منوها از دیتابیس لود می‌شوند (`Menu.model.js`)
  - آیکون‌ها با تابع `getMenuIcon()` مدیریت می‌شوند
  - ساختار درختی (Tree Structure) برای منوهای والد و فرزند
  - **نمایش چند سطحی در Sidebar**: ✨ جدید!
    - منوها و زیرمنوها به صورت درختی (collapsible/expandable)
    - دکمه ▼ برای باز کردن زیرمنو، ◀ برای بسته کردن
    - Indent اتوماتیک برای سطوح مختلف
    - انیمیشن smooth برای باز/بسته شدن
    - استایل متفاوت برای منوی والد و زیرمنو
    - بوردر راست برای نمایش سلسله مراتب
- ✅ **تنظیمات سیستم (System Settings)**:
  - منوی جدید: "تنظیمات سیستم" (`settings.system`)
  - زیر منو 1: "مدیریت منوها" (`/admin/settings/menus`)
  - زیر منو 2: "همگام‌سازی API" (`/admin/sync-apis`)
- ✅ **صفحه مدیریت منوها** (`/admin/settings/menus`):
  - نمایش منوها به صورت درختی (Tree View) - چند سطحی
  - افزودن منوی جدید (Create)
  - ویرایش منو (Edit)
  - حذف منو (Delete) با بررسی زیرمنوها
  - **جانمایی مجدد منوها (Reordering)**: ✨ جدید!
    - دکمه ↑ : انتقال به بالا (Swap Order)
    - دکمه ↓ : انتقال به پایین (Swap Order)
    - دکمه → : افزایش سطح (تبدیل به زیرمنو)
    - دکمه ← : کاهش سطح (تبدیل به منوی اصلی)
  - پشتیبانی از چند سطح نامحدود (منو، زیرمنو، زیر-زیرمنو، ...)
  - UI زیبا با دکمه‌های جابجایی گروه‌بندی شده
  - Responsive design برای موبایل
  - فیلدها: menuId, title, titleEn, path, parentId, icon, order, isActive
- ✅ **API Routes**:
  - `GET /api/admin/settings/menus` - لیست تمام منوها
  - `POST /api/admin/settings/menus` - ایجاد منوی جدید
  - `GET /api/admin/settings/menus/:id` - دریافت یک منو
  - `PUT /api/admin/settings/menus/:id` - ویرایش منو
  - `DELETE /api/admin/settings/menus/:id` - حذف منو
  - **`GET /api/user/menus` - دریافت منوها به صورت Tree**: ✨ بهبود یافت!
    - تبدیل اتوماتیک به ساختار درختی
    - اضافه کردن parent های missing به صورت recursive
    - پشتیبانی از چند سطح نامحدود
- ✅ **Activity Log**: اضافه شدن actions:
  - `settings.menus.list`
  - `menu.create`
  - `menu.update`
  - `menu.delete`
- ✅ **RBAC**: admin دسترسی کامل به تمام منوهای تنظیمات

---

#### حذف "مجوزها" و اضافه کردن عملیات مدیریت کاربر

- ✅ **حذف شد**: دکمه و modal "مجوزها" (Permissions) - چون الان سیستم نقش (Roles) داریم
- ✅ **اضافه شد**: سه عملیات جدید در صفحه مدیریت کاربران:

  1. **غیرفعال کردن (Suspend/Unsuspend)**:
     - دکمه زرد (warning) با آیکون مناسب
     - toggle بین suspended و active
     - Confirm dialog قبل از اجرا
  2. **حذف کاربر (Delete)**:
     - دکمه قرمز (danger) با آیکون سطل زباله
     - Confirm dialog با هشدار "غیرقابل بازگشت"
     - استفاده از API موجود: `DELETE /api/admin/users/:id`

- ✅ **API جدید**: `PUT /api/admin/users/:id/state`
  - تغییر state کاربر (active, suspended, verified, etc.)
  - محافظت شده با RBAC (admin و moderator)
  - جلوگیری از تغییر state admin توسط moderator
  - ثبت activity log کامل
- ✅ **UI/UX**:
  - دکمه‌ها با رنگ‌بندی واضح (آبی، زرد، قرمز)
  - disabled state برای جلوگیری از چندین کلیک همزمان
  - tooltip برای هر دکمه
  - آیکون‌های متفاوت برای suspended و active
- ✅ **Activity Log**: اضافه شدن `user_state_change` به enum

---

### 🐛 رفع مشکلات (Bug Fixes)

#### رفع مشکل بکگراند شفاف در `EditUserModal`

- ✅ **مشکل**: بخش‌هایی از مدال (header, tabs, body, footer, inputs) بکگراند transparent داشتند
- ✅ **راه‌حل**: اضافه شدن **fallback colors** برای CSS variables:
  - **Modal**: `background: var(--background-primary, #ffffff)` + dark mode fallback
  - **Header**: `background: var(--background-primary, #ffffff)` + dark mode fallback
  - **Tabs**: `background: var(--background-secondary, #f9fafb)` + dark mode fallback
  - **Body**: `background: var(--background-primary, #ffffff)` + dark mode fallback
  - **Footer**: `background: var(--background-primary, #ffffff)` + dark mode fallback
  - **Inputs/Textarea/Select**: `background: var(--background-primary, #ffffff)` + dark mode fallback
  - **Buttons**: fallback colors برای cancel و submit
- ✅ **نتیجه**: مدال حتی بدون تعریف CSS variables هم رنگ دارد و در همه تم‌ها کار می‌کند

---

#### رفع مشکل نمایش منوهای جدید در `/admin/settings/menus`

- ✅ **مشکل**: بعد از ساخت، ویرایش، یا جابجایی منو، لیست منوها به‌روز نمی‌شد
- ✅ **راه‌حل**: اضافه کردن `await` به `fetchMenus()` در تمام توابع CRUD
- ✅ **توابع اصلاح شده**:
  - `handleSubmit` - بعد از create/edit
  - `handleMoveUp`/`handleMoveDown` - بعد از جابجایی
  - `handleIndent`/`handleOutdent` - بعد از تغییر سطح
- ✅ **نتیجه**: لیست منوها بلافاصله بعد از هر تغییر، به‌روز می‌شود

---

#### رفع مشکل infinite loop در `/api/user/menus`

- ✅ **مشکل**: تابع `addMissingParents` در حالتی که parent در دیتابیس وجود نداشت، تو infinite loop می‌افتاد
- ✅ **راه‌حل**:
  - اضافه شدن چک `parentMenus.length === 0` برای خروج از loop
  - اضافه شدن محدودیت `maxDepth = 10` برای امنیت بیشتر
  - بهبود logging برای debug راحت‌تر
- ✅ **نتیجه**: API در حداکثر 10 سطح recursive می‌شود و دیگه hang نمی‌کند

---

#### رفع مشکل backslash در path های API در ویندوز

- ✅ **مشکل**: در سیستم‌های ویندوز، `path.relative()` از backslash (`\`) استفاده می‌کرد و path های API به صورت `/api/admin\rbac\roles` نمایش داده می‌شدند
- ✅ **راه‌حل**: اضافه کردن `.replace(/\\/g, "/")` برای تبدیل backslash به forward slash
- ✅ **فایل‌های اصلاح شده**:
  - `scripts/sync-api-endpoints.mjs`
  - `src/app/api/admin/sync-apis/route.js`
- ✅ **نتیجه**: حالا همه path ها با `/` نمایش داده می‌شوند، صرف نظر از سیستم‌عامل

#### اصلاح ساختار درختی API Permission Tree

- ✅ **مشکل**: ساختار درختی به درستی چند سطحی نبود و همه endpoint ها در یک سطح نمایش داده می‌شدند
- ✅ **راه‌حل**: اصلاح الگوریتم `buildTree` برای ساخت ساختار درختی واقعی چند سطحی
- ✅ **تغییرات**:
  - استفاده از `children` به جای اضافه کردن endpoint ها در یک سطح
  - Navigation درست بین سطوح مختلف tree
  - استفاده از `node.path` در `expandAll` بجای ساخت path جدید
- ✅ **نتیجه**: حالا ساختار کاملاً چند سطحی و مشابه file explorer است

---

### ✨ ویژگی‌های جدید (New Features)

#### 🤖 همگام‌سازی خودکار API Endpoints (Auto API Sync) ⭐⭐⭐

- ✅ **اسکریپت خودکار**: `npm run sync-apis`
  - اسکن خودکار تمام فایل‌های `route.js` در پروژه
  - تشخیص متدهای HTTP (GET, POST, PUT, DELETE)
  - اضافه/آپدیت خودکار به collection `ApiEndpoint`
  - تعیین هوشمند `defaultRoles` براساس مسیر API
  - پشتیبانی از dynamic routes (`[id]` → `:id`)
- ✅ **UI Panel**: `/admin/sync-apis`
  - دکمه ساده برای همگام‌سازی
  - نمایش آمار (اضافه شده، آپدیت شده، بدون تغییر)
  - لیست کامل endpoint ها و متدها
  - راهنمای گام‌به‌گام
- ✅ **API Endpoint**: `POST /api/admin/sync-apis`
  - قابل فراخوانی از خارج
  - محافظت شده (فقط admin)
  - خروجی JSON کامل
- ✅ **مستندات کامل**: `docs/API_SYNC.md`
  - توضیح کامل نحوه کار
  - مثال‌های کاربردی
  - عیب‌یابی و troubleshooting

#### 🌳 ساختار درختی API Permissions (API Permission Tree) ⭐⭐⭐

- ✅ **کامپوننت جدید**: `ApiPermissionTree`
  - جایگزین `ApiPermissionGrid` با UX بهتر
  - **ساختار درختی چند سطحی** براساس path hierarchy (کاملاً مشابه ساختار فایل‌ها و پوشه‌ها)
  - دکمه‌های "باز کردن همه" و "بستن همه"
  - آیکون‌های 📁 (فولدر بسته), 📂 (فولدر باز), 📄 (endpoint/فایل)
  - هر endpoint به صورت یک فایل در زیر پوشه‌های مربوطه
  - **Indentation واقعی** برای نمایش سطوح مختلف (depth-based padding)
- ✅ **فیلتر براساس نوع متد** ⭐ جدید!
  - دکمه‌های رنگی برای فیلتر GET, POST, PUT, DELETE
  - کلیک روی متد → فقط endpoint های با اون متد نمایش داده میشن
  - امکان انتخاب چندین متد همزمان
  - دکمه "✕" برای پاک کردن فیلتر
  - رنگ‌بندی: 🔵 GET | 🟢 POST | 🟠 PUT | 🔴 DELETE
- ✅ **بهبود UX**:
  - سازمان‌یافته‌تر و خیلی کمتر شلوغ
  - پیدا کردن API ها سریع‌تر
  - Search با فیلتر هوشمند درختی
  - انتخاب متدها به صورت بصری و واضح
  - Toolbar کامل با search, filter و actions
- ✅ **مثال ساختار** (چند سطحی واقعی):
  ```
  📁 api
    📁 auth
      📄 /api/auth/login (POST)
      📄 /api/auth/send-otp (POST)
      📄 /api/auth/verify-otp (POST)
    📁 admin
      📁 users
        📄 /api/admin/users (GET, POST)
        📁 :id
          📄 /api/admin/users/:id (GET, PUT, DELETE)
          📁 roles
            📄 /api/admin/users/:id/roles (PUT)
      📁 rbac
        📁 roles
          📄 /api/admin/rbac/roles (GET, POST)
          📁 :id
            📄 /api/admin/rbac/roles/:id (GET, PUT, DELETE)
        📄 /api/admin/rbac/menus (GET, POST)
        📄 /api/admin/rbac/apis (GET, POST)
    📁 user
      📄 /api/user/profile (GET, PUT)
  ```

#### 🔍 صفحه‌های Debug برای بررسی دسترسی‌ها

- ✅ **مسیر 1: `/admin/debug`**

  - نمایش اطلاعات کاربر (شماره موبایل، نقش‌ها، ID)
  - لیست تمام نقش‌های موجود در دیتابیس
  - تست دسترسی به endpoint خاص: `PUT /api/admin/users/:id/roles`
  - نمایش جدولی از تمام دسترسی‌های API
  - دکمه‌های سریع: بروزرسانی و اجرای seed

- ✅ **مسیر 2: `/admin/debug-permissions`** (جدید ⭐)
  - نمایش **دقیق** تمام دسترسی‌های کاربر فعلی
  - تفکیک شده براساس نقش‌ها
  - لیست کامل API Permissions با path و methods
  - لیست کامل Menu Permissions
  - **بهترین راه برای debugging مشکلات 403!**

#### 🎯 نقش پایه `user` برای همه کاربران

- ✅ **تضمین وجود نقش `user`**
  - نقش `user` به عنوان نقش پایه برای تمام کاربران اجباری است
  - در API route تغییر نقش، خودکار اضافه می‌شود اگر حذف شده باشد
  - در `RolesModal` به صورت disabled نمایش داده می‌شود
  - Badge "پایه" برای مشخص کردن این نقش
  - Tooltip توضیحی: "نقش پایه - همه کاربران باید این نقش را داشته باشند"

#### 🗂️ نمایش منوها براساس مجوزات (Dynamic Menu Filtering)

- ✅ **API Endpoint جدید: `/api/user/menus`**
  - دریافت منوهای مجاز براساس نقش‌های کاربر
  - استفاده از `getAllowedMenus` از middleware RBAC
  - کش نشدن اطلاعات برای real-time updates
- ✅ **فیلتر خودکار Sidebar**
  - Admin همه منوها را می‌بیند
  - سایر کاربران فقط منوهای مجاز را می‌بینند
  - بدون نیاز به hard-code کردن منوها
  - تغییرات مجوزات بدون restart سرور اعمال می‌شوند

#### 🔒 محافظت از API Routes با RBAC دینامیک (API Protection)

- ✅ **Middleware جدید: `protectApi`**
  - محافظت یکپارچه: احراز هویت + بررسی نقش + بررسی مجوز
  - بررسی خودکار دسترسی از دیتابیس (Role.apiPermissions)
  - Logging خودکار برای audit trail
  - پشتیبانی از fallback roles
- ✅ **ویژگی‌ها:**
  - Admin همیشه دسترسی کامل دارد
  - Deny by default برای امنیت بیشتر
  - پشتیبانی از endpoints عمومی (isPublic)
  - بررسی method-level (GET, POST, PUT, DELETE)
  - تغییرات مجوزات بدون restart اعمال می‌شوند
- ✅ **مثال استفاده:**
  - آپدیت `/api/admin/users` با `protectApi`
  - کد تمیزتر: 3 خط به جای 10 خط
  - Error handling بهتر با status codes صحیح

#### 🔐 سیستم RBAC دینامیک (Dynamic Role-Based Access Control)

- ✅ **معماری سه‌لایه RBAC**
  - ایجاد، ویرایش و حذف نقش‌ها بدون نیاز به تغییر کد
  - کنترل دسترسی به منوهای پنل (Menu Permissions)
  - کنترل دسترسی به API Endpoints با سطح متد HTTP
  - نقش‌های سیستمی محافظت شده (غیرقابل حذف)
  - **5 نقش پیش‌فرض**: `admin`, `moderator`, `event_owner`, `user`, `guest`
- ✅ **مدل‌های پایگاه داده**
  - `Role.model.js`: مدیریت نقش‌ها با `menuPermissions` و `apiPermissions`
  - `Menu.model.js`: ساختار درختی منوها با پشتیبانی از parent/child
  - `ApiEndpoint.model.js`: مدیریت API endpoints با متدهای مختلف
- ✅ **API Routes کامل**
  - `/api/admin/rbac/roles`: CRUD کامل برای نقش‌ها (GET, POST, PUT, DELETE)
  - `/api/admin/rbac/roles/:id`: مدیریت تک نقش
  - `/api/admin/rbac/menus`: دریافت منوها (با گزینه نمایش درختی)
  - `/api/admin/rbac/apis`: دریافت API endpoints (با گزینه گروه‌بندی)
  - `/api/admin/rbac/seed`: ایجاد خودکار داده‌های اولیه
- ✅ **UI مدیریت نقش‌ها**
  - **صفحه لیست نقش‌ها** (`/admin/rbac/roles`)
    - نمایش کارتی نقش‌ها با طراحی مدرن
    - فیلتر بر اساس نوع (همه، سیستمی، سفارشی)
    - جستجوی لحظه‌ای در نقش‌ها
    - نمایش آمار (تعداد کل، سیستمی، سفارشی)
    - دکمه‌های ویرایش و حذف
  - **صفحه ویرایش/ایجاد نقش** (`/admin/rbac/roles/:id` و `/admin/rbac/roles/new`)
    - سه تب: اطلاعات پایه، دسترسی منوها، دسترسی API
    - فرم اعتبارسنجی شده برای اطلاعات پایه
    - Auto-generate slug از نام نقش
    - هشدار برای نقش‌های سیستمی
    - Badge نمایش تعداد دسترسی‌ها در تب‌ها
- ✅ **کامپوننت MenuTreeView**
  - نمایش درختی منوها با قابلیت باز/بسته شدن (expand/collapse)
  - انتخاب سطح دسترسی: 👁️ مشاهده یا 🔒 دسترسی کامل
  - Auto-expand برای نودهای دارای دسترسی
  - آیکون‌های زیبا برای هر منو
  - نمایش مسیر (path) هر منو
  - Legend توضیحی برای راهنمایی کاربر
- ✅ **کامپوننت ApiPermissionGrid**
  - گروه‌بندی API endpoints بر اساس ماژول
  - انتخاب متدهای HTTP: GET, POST, PUT, DELETE
  - رنگ‌بندی متدها (آبی، سبز، نارنجی، قرمز)
  - دکمه "انتخاب همه" برای هر endpoint
  - جستجوی لحظه‌ای در API ها
  - نمایش آمار (تعداد endpoint ها و متدها)
  - باز/بسته شدن هر ماژول
- ✅ **Seeding System**
  - اسکریپت `seed-rbac.mjs` برای ایجاد داده‌های اولیه
  - API Route `/api/admin/rbac/seed` برای اجرا از UI
  - صفحه Admin (`/admin/rbac/seed`) با دکمه اجرا
  - ایجاد 12 منوی پیش‌فرض
  - ایجاد 12 API endpoint پیش‌فرض
  - ایجاد 3 نقش سیستمی (Admin, Event Owner, User)
- ✅ **امنیت و لاگینگ**
  - محافظت تمام API ها با middleware احراز هویت
  - فقط Admin ها دسترسی به مدیریت RBAC دارند
  - جلوگیری از حذف نقش‌های سیستمی
  - Activity Logging برای تمام عملیات RBAC
  - اعتبارسنجی ورودی‌ها (Validation)
- ✅ **مستندات کامل**
  - `docs/RBAC_DYNAMIC_SYSTEM.md`: راهنمای جامع سیستم RBAC
  - معماری و لایه‌های سیستم
  - توضیح مدل‌های داده
  - راهنمای استفاده از API
  - راهنمای استفاده از UI
  - مثال‌های کاربردی
  - نکات امنیتی و بهترین شیوه‌ها
- ✅ **یکپارچه‌سازی با Admin Panel**
  - اضافه کردن لینک "نقش‌ها و دسترسی‌ها" به sidebar
  - آیکون قفل برای RBAC
  - Highlight شدن در صورت فعال بودن صفحه RBAC

### 🔧 رفع باگ‌ها (Bug Fixes)

- ✅ **تغییر اساسی: Dynamic RBAC مستقل از ApiEndpoint Collection**
  - مشکل: `checkApiPermission` به `ApiEndpoint` collection وابسته بود
  - علت: دسترسی‌هایی که از UI پنل ادمین داده می‌شدند، فقط در `apiPermissions` نقش ذخیره می‌شدند
  - راه‌حل: **حذف کامل وابستگی به `ApiEndpoint`**
    - ✅ `checkApiPermission` الان **فقط** از `apiPermissions` نقش‌ها چک می‌کنه
    - ✅ دیگه نیازی به seed کردن `ApiEndpoint` نیست
    - ✅ همه دسترسی‌ها **داینامیک** از UI پنل ادمین قابل مدیریت هستند
  - تغییرات:
    - ✅ بازنویسی کامل `checkApiPermission` در `dynamicRbac.js`
    - ✅ اضافه کردن logging کامل برای debugging
    - ✅ تمام endpoint ها: `checkPermission: true`
  - نتیجه: **هر دسترسی که از UI بدی، فوری اعمال میشه!** ✨
- ✅ **بهبود نمایش خطاها در RolesModal**
  - اضافه کردن نمایش خطاهای دقیق بر اساس `code` و `status`:
    - ✅ `403` → "⛔ شما مجوز تغییر نقش کاربران را ندارید"
    - ✅ `401` → "🔒 لطفاً ابتدا وارد شوید"
    - ✅ `400 + INVALID_ROLES` → "❌ نقش‌های نامعتبر: ..."
    - ✅ `400 + SELF_ADMIN_REMOVAL` → "نمی‌توانید نقش مدیریت را از خودتان حذف کنید"
  - بهبود logging برای debugging
  - اضافه کردن emoji ها برای وضوح بیشتر
- ✅ **رفع خطای 403 برای `/api/admin/users/:id/roles`**
  - مشکل: حتی با نقش `moderator` نمی‌توانستند نقش کاربران را تغییر دهند
  - علت: این API route از `authenticate` و `requireRole` استفاده می‌کرد، نه `protectApi`
  - راه‌حل:
    - ✅ تغییر `/api/admin/users/[id]/roles/route.js` به `protectApi`
    - ✅ اضافه کردن `code` به تمام error responses برای تشخیص بهتر نوع خطا
    - ✅ اضافه کردن `moderator` به `defaultRoles` endpoint در seed files
    - ✅ اضافه کردن endpoint به `apiPermissions` نقش `moderator`
- ✅ **رفع خطای 403 برای `/api/admin/rbac/roles`**
  - مشکل: حتی با نقش `moderator` به API نقش‌ها دسترسی نداشتند
  - علت 1: این API route از `protectApi` استفاده نمی‌کرد، فقط `admin` رو چک می‌کرد
  - علت 2: endpoint `/api/admin/rbac/roles` در `apiPermissions` نقش `moderator` نبود
  - راه‌حل:
    - ✅ تغییر `/api/admin/rbac/roles/route.js` به `protectApi` با fallback
    - ✅ اضافه کردن endpoint به `scripts/seed-rbac.mjs` با `defaultRoles: ["admin", "moderator"]`
    - ✅ اضافه کردن به `apiPermissions` نقش `moderator` در هر دو seed file
- ✅ **رفع خطای ActivityLog در RBAC Logging**
  - مشکل: `api_access_granted` و `ApiEndpoint` در enum های ActivityLog نبودند
  - خطا: `ActivityLog validation failed: action: 'api_access_granted' is not a valid enum value`
  - راه‌حل: اضافه کردن مقادیر جدید:
    - ✅ `action` enum: `api_access_granted`, `api_access_denied`
    - ✅ `targetType` enum: `ApiEndpoint`, `Menu`
- ✅ **رفع مشکل دسترسی API برای نقش‌های مجاز**
  - مشکل: حتی با `allowedRoles` معتبر، خطای "دسترسی غیرمجاز" می‌داد
  - علت: `checkApiPermission` وقتی endpoint در دیتابیس نبود، بلافاصله deny می‌کرد
  - راه‌حل 1: اضافه کردن پشتیبانی از `defaultRoles` در ApiEndpoint
  - راه‌حل 2: تغییر منطق `protectApi` به fallback:
    - ✅ ابتدا RBAC دیتابیس چک می‌شود
    - ✅ اگر موفق بود، اجازه داده می‌شود
    - ✅ اگر ناموفق بود، به `allowedRoles` fallback می‌زند
    - ✅ این باعث می‌شود endpoints seed نشده هم با `allowedRoles` کار کنند
  - بهبود logging برای debugging بهتر
- ✅ **رفع مشکل نمایش منوها برای نقش‌های غیر admin**
  - مشکل: `getAllowedMenus` هنوز از `perm.canView` استفاده می‌کرد (schema قدیمی)
  - راه‌حل: تغییر به `perm.access` (schema جدید)
  - مشکل: `getAllowedApis` از `perm.endpoint` استفاده می‌کرد
  - راه‌حل: تغییر به `perm.path` (schema جدید)
  - بهبود API `/api/user/menus` برای برگرداندن اطلاعات کامل منوها
  - اضافه کردن logging برای debugging راحت‌تر
- ✅ **رفع خطای Dynamic Roles در User Model**
  - مشکل: `User.model.js` دارای enum ثابت برای `roles` بود
  - این مانع از اضافه کردن نقش‌های جدید توسط Admin می‌شد
  - خطا: `User validation failed: roles.0: 'new_role' is not a valid enum value`
  - راه‌حل: حذف `enum` از `roles` در User.model
  - Validation دینامیک در API route از طریق `Role.model` انجام می‌شود
  - حالا Admin می‌تواند نقش‌های جدید بسازد و به کاربران اختصاص دهد
- ✅ **رفع خطای Seed API Route**
  - مشکل: فرمت `apiPermissions` و `menuPermissions` در `/api/admin/rbac/seed` با Schema سازگار نبود
  - استفاده از `canView` به جای `access` در `menuPermissions`
  - استفاده از `endpoint` به جای `path` در `apiPermissions`
  - راه‌حل: آپدیت کردن `seed/route.js` با فرمت جدید Schema
  - اضافه کردن نقش‌های `moderator` و `guest` به Seed API
- ✅ **رفع مشکل import `connectDB`**
  - تغییر از `import { connectDB }` به `import connectDB` در همه API routes
  - حل خطای "connectDB is not a function"
- ✅ **مهاجرت به NewAuthContext**
  - تبدیل تمام صفحات و کامپوننت‌ها از `AuthContext` قدیمی به `NewAuthContext`
  - حل مشکل احراز هویت با Cookie-based authentication
  - بهبود امنیت و performance
- ✅ **رفع مشکل نمایش نقش‌های جدید در User Management**
  - آپدیت `RolesModal` برای استفاده از API دینامیک `/api/admin/rbac/roles`
  - آپدیت `/api/admin/roles` برای استفاده از `Role.model.js` دینامیک
  - آپدیت `/api/admin/users/[id]/roles` برای validation دینامیک نقش‌ها

### 📝 تغییرات فنی (Technical Changes)

- ✅ **مدل‌های Mongoose جدید**
  - `Role.model.js` با متدهای `hasMenuAccess` و `hasApiAccess`
  - `Menu.model.js` با متدهای استاتیک برای ساختار درختی
  - `ApiEndpoint.model.js` با پشتیبانی از چندین متد HTTP
- ✅ **CSS Modules**
  - `roleEdit.module.css`: استایل صفحه ویرایش/ایجاد نقش
  - `MenuTreeView.module.css`: استایل کامپوننت درخت منوها
  - `ApiPermissionGrid.module.css`: استایل کامپوننت جدول API
  - طراحی responsive و مدرن
  - انیمیشن‌ها و transition های روان
- ✅ **بهینه‌سازی Performance**
  - استفاده از `useState` و `useEffect` برای مدیریت state
  - Lazy loading برای داده‌های بزرگ
  - Memoization در کامپوننت‌ها

## [نسخه قبلی] - 2025-10-27

### ✨ ویژگی‌های جدید (New Features)

#### 🎭 انتخاب نقش در ثبت‌نام

- ✅ **انتخاب نوع حساب با UI دکمه‌ای**
  - تغییر از dropdown به دکمه‌های انتخابی برای UX بهتر
  - دو گزینه: 👤 کاربر عادی (User) یا ⭐ مالک/مدیر رویداد (Event Owner)
  - نمایش ایموجی، عنوان و توضیحات در هر دکمه
  - Hover effects و Active state با رنگ و سایه
  - Responsive: دکمه‌ها در موبایل به صورت عمودی
  - کاربر از همان ابتدا می‌داند چه نوع حسابی ایجاد می‌کند
  - برای همه کاربران نمایش داده می‌شود (جدید و قدیمی)
- ✅ **آپدیت API verify-otp**
  - دریافت پارامتر `role` از فرانت‌اند
  - تخصیص خودکار نقش‌های مناسب هنگام ثبت‌نام
  - نقش پیش‌فرض: `["user"]`
  - با انتخاب Event Owner: `["user", "event_owner"]` (هر دو نقش)
  - نقش‌ها در `User.toPublicJSON()` برگردانده می‌شوند
  - Frontend نقش‌ها را در localStorage ذخیره می‌کند
- ✅ **ارتقا خودکار نقش (Role Upgrade)**
  - اگر کاربر قدیمی با نقش `event_owner` وارد شود، نقش اضافه می‌شود
  - مثال: `["user"]` → `["user", "event_owner"]`
  - Logging برای ردیابی تغییرات نقش
  - فقط upgrade امکان‌پذیر است، downgrade خودکار نیست (امنیتی)
- ✅ **آپدیت AuthContext**
  - اضافه کردن پارامتر `role` به `loginWithOTP`
  - برگرداندن `isNewUser` از `sendOTP`
- ✅ **بهبود UI صفحه لاگین**
  - CSS جدید برای دکمه‌های انتخاب نقش
  - Grid layout با 2 دکمه
  - Hover effects و Active state
  - طراحی مدرن و responsive
  - انیمیشن fade-in و translateY
- ✅ **ابزارهای دیباگ و تست**
  - اسکریپت `check-user-roles.js` برای بررسی نقش‌های کاربران
  - دستور `npm run check-user [phoneNumber]`
  - Logging در API برای ردیابی نقش‌ها
  - مستند کامل `docs/TESTING_ROLES.md`
- ⚠️ **محدودیت نقش Moderator**
  - نقش Moderator فقط توسط Admin قابل تخصیص است
  - کاربران عادی نمی‌توانند این نقش را انتخاب کنند

#### 🎛️ مدیریت نقش‌ها در پنل ادمین

- ✅ **Modal مدیریت نقش‌ها (`RolesModal`)**
  - UI تعاملی با Checkbox برای انتخاب نقش‌ها
  - نمایش لیست تمام نقش‌های موجود با ایموجی و توضیحات
  - امکان اضافه/حذف چندین نقش به صورت همزمان
  - نمایش اطلاعات کاربر (آواتار، نام، شماره)
  - Loading و Error handling کامل
  - طراحی Responsive برای موبایل و تبلت
- ✅ **دکمه مدیریت نقش‌ها**
  - اضافه شدن به لیست کاربران در پنل ادمین
  - آیکون مخصوص نقش‌های کاربر
  - Tooltip برای راهنمایی
- ✅ **Auto-refresh لیست کاربران**
  - بعد از تغییر نقش‌ها، لیست خودکار به‌روزرسانی می‌شود
  - نمایش نقش‌های جدید بدون نیاز به رفرش صفحه

#### 🔐 مدیریت رمز عبور کاربران

- ✅ **Modal تنظیم رمز عبور (`PasswordModal`)** - توسط Admin
  - UI زیبا و کاربرپسند برای تنظیم رمز
  - نمایش اطلاعات کاربر (آواتار، نام، شماره)
  - فیلد رمز عبور با امکان نمایش/پنهان کردن
  - تکرار رمز عبور برای جلوگیری از اشتباه
  - نشانگر قدرت رمز (ضعیف/متوسط/قوی)
  - Validation: حداقل 6 کاراکتر
  - پیام موفقیت و بستن خودکار Modal
  - طراحی Responsive
- ✅ **API Route تنظیم رمز (`/api/admin/users/[id]/password`)**
  - تنظیم/تغییر رمز عبور توسط Admin
  - Hash کردن رمز با bcrypt
  - Validation کامل
  - Activity Logging
  - Error handling بهتر
- ✅ **دکمه تنظیم رمز عبور**
  - اضافه شدن به لیست کاربران
  - آیکون کلید برای تنظیم رمز
  - Tooltip راهنما

#### 🔑 صفحه فراموشی رمز عبور

- ✅ **صفحه Forgot Password (`/forgot-password`)**

  - UI مدرن و زیبا با 3 مرحله (Step-by-step)
  - مرحله 1: ورود شماره موبایل
  - مرحله 2: تایید کد OTP
  - مرحله 3: تنظیم رمز جدید
  - Progress indicator نمایش پیشرفت
  - نشانگر قدرت رمز
  - امکان ارسال مجدد OTP با countdown
  - پیام‌های موفقیت و خطا
  - Redirect خودکار به صفحه ورود بعد از موفقیت
  - طراحی Responsive
  - انیمیشن‌های نرم

- ✅ **API Routes فراموشی رمز**

  - `/api/auth/verify-otp-forgot` - تایید OTP برای فراموشی رمز
  - `/api/auth/reset-password` - بازنشانی رمز عبور
  - استفاده از `global.otpStore` برای ذخیره موقت OTP
  - Validation کامل
  - بررسی وجود کاربر
  - Hash کردن رمز جدید
  - Activity Logging
  - Error handling

- ✅ **یکپارچگی با سیستم OTP موجود**
  - استفاده از همان سرویس SMS.ir
  - OTP Store مشترک با login
  - Countdown 2 دقیقه
  - امکان ارسال مجدد

#### 👤 صفحه پروفایل کاربر

- ✅ **صفحه Profile (`/profile`)**

  - UI زیبا و مدرن با Grid Layout (Sidebar + Main Content)
  - Sidebar: آواتار/لوگو، اطلاعات اولیه، دکمه تغییر رمز
  - Main Content: فرم اطلاعات شخصی با حالت View/Edit
  - نمایش اطلاعات به صورت Read-Only با دکمه ویرایش
  - حالت ویرایش با دکمه‌های لغو و ذخیره
  - Theme Toggle برای تغییر تم
  - طراحی Responsive (موبایل، تبلت، دسکتاپ)
  - انیمیشن‌های smooth (slideDown, fadeIn)

- ✅ **فیلدهای Dynamic بر اساس نوع کاربر**

  - **👤 شخصی (Individual):** نام، نام خانوادگی، نام نمایشی، بیوگرافی، ایمیل، لوکیشن، شبکه‌های اجتماعی
  - **🏢 سازمانی (Organization):** نام سازمان/برند (الزامی), نام مدیر, شناسه ملی/ثبت, وب‌سایت, لوگو, شبکه‌های اجتماعی
  - **🏛 دولتی (Government):** مشابه سازمانی با فیلدهای مخصوص نهادهای دولتی
  - UI دکمه‌ای برای انتخاب userType با ایموجی و توضیحات
  - تغییر فیلدها به صورت automatic بعد از انتخاب نوع

- ✅ **آپلود آواتار/لوگو**

  - آپلود تصویر با کلیک روی دکمه 📷 تغییر تصویر
  - نمایش پیش‌نمایش (Preview) در لحظه
  - بررسی نوع فایل (image only) و حجم (max 2MB)
  - تبدیل به base64 (در production: آماده برای S3/Cloudinary)
  - پیام‌های موفقیت و خطا
  - نمایش Spinner در حین آپلود
  - برای کاربران سازمانی/دولتی: آپلود لوگو به جای آواتار

- ✅ **Modal تغییر رمز عبور**

  - دکمه تغییر رمز در Sidebar با آیکون 🔒
  - Modal مدرن با Overlay و blur backdrop
  - فیلدها: رمز فعلی، رمز جدید، تکرار رمز جدید
  - نمایش/پنهان کردن رمز با آیکون 👁
  - نشانگر قدرت رمز (ضعیف 🔴 / متوسط 🟠 / قوی 🟢)
  - Validation کامل (حداقل 6 کاراکتر، تطابق تکرار، تفاوت با رمز قبلی)
  - پیام‌های موفقیت و خطا
  - بستن خودکار بعد از موفقیت (2 ثانیه)
  - Animation: fadeIn و slideUp

- ✅ **API Routes پروفایل**

  - **GET `/api/user/profile`** - دریافت اطلاعات پروفایل کاربر (Authentication required)
  - **PUT `/api/user/profile`** - به‌روزرسانی اطلاعات پروفایل (Validation برای userType, Activity Logging)
  - **POST `/api/user/upload-avatar`** - آپلود آواتار/لوگو (بررسی نوع و حجم، base64 support)
  - **PUT `/api/user/change-password`** - تغییر رمز عبور (بررسی رمز فعلی، bcrypt hash، Activity Logging)
  - پشتیبانی از کاربران بدون رمز (فقط OTP login)

- ✅ **Activity Logging**

  - اضافه شدن `profile_update` به enum های ActivityLog
  - ثبت تغییرات پروفایل با metadata (فیلدهای تغییر یافته)
  - ثبت آپلود آواتار/لوگو (حجم فایل، نوع)
  - ثبت تغییر رمز عبور (method, IP address)

- ✅ **اطلاعات اضافی**
  - نمایش شماره موبایل (direction: ltr)
  - نمایش وضعیت (Badge): فعال, تایید شده, در انتظار تایید
  - نمایش نقش‌ها (Badges): کاربر, مالک رویداد, ناظر, مدیر
  - شبکه‌های اجتماعی: 📸 اینستاگرام, ✈️ تلگرام, 🐦 توییتر, 💼 لینکدین
  - لوکیشن: استان، شهر

#### 🆙 ارتقا نقش کاربر (Self-Upgrade)

- ✅ **Card ارتقا نقش در پروفایل**

  - نمایش پیشنهاد ارتقا به مالک رویداد
  - دکمه "⭐ فعال‌سازی نقش مالک رویداد"
  - فقط برای کاربرانی که نقش event_owner ندارند
  - UI زیبا با gradient و animation
  - توضیحات درباره مزایای نقش مالک رویداد

- ✅ **API Route ارتقا نقش (`/api/user/upgrade-role`)**
  - Self-upgrade به event_owner
  - بررسی وجود نقش قبلی
  - Activity Logging کامل
  - Error handling
  - به‌روزرسانی خودکار پروفایل بعد از ارتقا

#### 🧭 Navigation و دسترسی به پروفایل

- ✅ **Header/Navigation ثابت در صفحه اصلی**

  - لوگو PlusMeet (کلیک → برگشت به خانه)
  - لینک پروفایل 👤 (برای کاربران لاگین شده)
  - لینک پنل مدیریت ⚙️ (فقط برای Admin)
  - دکمه خروج (برای کاربران لاگین شده)
  - دکمه ورود/ثبت‌نام (برای مهمان‌ها)
  - Fixed position با backdrop blur
  - Responsive برای موبایل
  - تغییر رنگ در تم تیره

- ✅ **لینک پروفایل در پنل ادمین**
  - کلیک روی نام کاربری در sidebar → انتقال به صفحه پروفایل
  - آیکون 👤 در کنار نام کاربری
  - Hover effect با border رنگی و سایه
  - Tooltip: "کلیک کنید برای مشاهده پروفایل 👤"
  - Transition نرم و زیبا

---

### 🔐 مهاجرت به httpOnly Cookie Authentication

#### معماری جدید احراز هویت

- ✅ استفاده از httpOnly Cookies به جای localStorage
- ✅ CSRF Protection با X-CSRF-Token
- ✅ Auto-refresh Token mechanism
- ✅ امنیت در برابر XSS attacks
- ✅ Production-ready authentication

#### API Routes آپدیت شده

- ✅ `/api/auth/verify-otp` - با Cookie support
- ✅ `/api/auth/login` - با Cookie support
- ✅ `/api/auth/refresh` - تمدید خودکار accessToken
- ✅ `/api/auth/logout` - پاک کردن cookies

#### NewAuthContext

- ✅ `fetchWithAuth()` - با auto-refresh
- ✅ `checkAuth()` - بررسی وضعیت
- ✅ Cookie-based authentication

#### RBAC System پیشرفته

- ✅ `API_ROUTES` config - مدیریت متمرکز endpoints
- ✅ `PERMISSIONS` config - تعریف دسترسی‌ها
- ✅ `usePermission` hook - بررسی دسترسی
- ✅ `ProtectedRoute` component - محافظت از routes
- ✅ صفحه 403 Forbidden

#### مستندات

- ✅ `docs/COOKIE_AUTH_GUIDE.md` - راهنمای کامل

---

### 🔧 رفع مشکلات امروز (Fixed Today)

#### رفع مشکلات Modal مدیریت نقش‌ها

- ✅ **رفع مشکل نمایش لیست نقش‌ها**

  - API endpoint حالا نقش‌های استاتیک برمی‌گرداند
  - اگر دیتابیس خالی باشد، از نقش‌های پیش‌فرض استفاده می‌شود
  - Logging بهتر برای دیباگ
  - نمایش پیام خطا در صورت عدم دریافت نقش‌ها

- ✅ **بهبود CSS Modal**

  - رفع مشکل header transparent
  - اضافه کردن background به header
  - بهبود border-radius برای header

- ✅ **بهبود تجربه کاربری**

  - نمایش پیام "هیچ نقشی یافت نشد" در صورت خالی بودن لیست
  - بهبود Error handling
  - اضافه کردن console logs برای دیباگ

- ✅ **رفع کامل مشکل Background های Transparent**
  - تنظیم background سفید (#ffffff) برای تمام بخش‌های Modal
  - Modal header: #ffffff
  - Modal body: #ffffff
  - User info section: #f8f9fa
  - Modal footer: #f8f9fa
  - Role items: #ffffff
  - پشتیبانی کامل از Dark Mode با رنگ‌های مخصوص

#### رفع خطای ActivityLog Validation

- ✅ **اضافه کردن Action های جدید به ActivityLog**

  - `users.list` - لیست کردن کاربران
  - `users.view` - مشاهده کاربر
  - `users.search` - جست‌وجوی کاربران
  - `users.export` - خروجی گرفتن از لیست کاربران
  - `users.roles.update` - به‌روزرسانی نقش‌های کاربر
  - `users.permissions.grant` - اعطای مجوز
  - `users.permissions.revoke` - لغو مجوز
  - `role_update` - تغییر نقش
  - `role_upgrade` - ارتقا خودکار نقش
  - `user_update` - ویرایش کاربر

- ✅ **بهبود API route آپدیت نقش‌ها**

  - استفاده از `_id` به جای `phoneNumber` برای شناسایی کاربر
  - Validation بهتر برای آرایه نقش‌ها
  - Logging کامل تغییرات (oldRoles, newRoles, addedRoles, removedRoles)
  - جلوگیری از حذف نقش Admin از خود
  - Error handling بهتر برای ActivityLog

- ✅ **بهبود targetType در ActivityLog**
  - اضافه شدن `User` (با U بزرگ) به enum
  - اضافه شدن `Event`, `Role`, `Permission`
  - پشتیبانی از هر دو فرمت (user/User, event/Event)

#### 🔐 رفع خطای "useAuth باید داخل AuthProvider استفاده شود"

- ✅ **بهبود AuthContext Initialization**
  - تغییر `createContext()` به `createContext(undefined)`
  - بهبود error checking در `useAuth` hook
- ✅ **بهبود Admin Layout**
  - استفاده از `authLoading` به جای `loading` محلی
  - حذف state اضافی و اتکا به Context loading
  - بهبود authentication flow
- ✅ **راهنمای دیباگ Authentication**
  - `docs/AUTH_DEBUGGING.md` با توضیحات کامل
  - راه‌حل‌های گام به گام
  - نکات مهم و best practices

#### 🐛 رفع خطای 403 در صفحه مدیریت کاربران

- ✅ **رفع مشکل `roles` در User Model**
  - اضافه کردن `roles` به متد `toPublicJSON()`
  - حالا کاربر پس از login، نقش‌هایش ذخیره می‌شود
- ✅ **رفع تناقض `status` vs `state`**
  - تغییر `user.status` به `user.state` در middleware
  - هماهنگ‌سازی با User Model
- ✅ **رفع مشکل `fetchWithAuth` در Frontend**
  - اضافه کردن `await response.json()` در `users/page.js`
  - حالا Response به درستی parse می‌شود
- ✅ **اسکریپت چک و تعمیر Admin**
  - `scripts/check-admin.js` برای بررسی کاربر admin
  - `npm run check-admin` برای تعمیر خودکار نقش‌های admin
- ✅ **رفع مشکل CommonJS vs ES Modules**
  - حذف `type: "module"` از package.json
  - تبدیل scripts به CommonJS
  - رفع خطای `module is not defined in ES module scope`

#### 🎨 رفع مشکل PWA Icons

- ✅ **ایجاد آیکون SVG موقت**
  - `favicon.svg` با لوگوی PM
  - گرادیانت Amber Gold → Golden Glow
- ✅ **به‌روزرسانی Manifest**
  - استفاده از SVG به جای PNG
  - رفع خطای "Download error or resource isn't a valid image"
- ✅ **راهنمای جامع آیکون‌سازی**
  - `public/icons/README.md` با دستورات کامل
  - لینک‌های placeholder برای آیکون سریع

#### 📚 مستندات جدید

- ✅ **راهنمای رفع مشکلات**
  - `docs/TROUBLESHOOTING.md` با حل مشکلات رایج
  - راهنمای دیباگ و بررسی سلامت سیستم

---

### اضافه شده (Added)

#### 🐛 رفع خطاهای مهم

- ✅ **بازنویسی Authentication Middleware برای App Router**
  - تبدیل `authenticate()` از Express به Next.js App Router
  - تبدیل `requireRole()` برای استفاده در API Routes
  - تبدیل `optionalAuth()` و `requireVerified()`
  - رفع خطای `TypeError: Cannot read properties of undefined`
  - تمام middleware ها حالا object برمی‌گردانند به جای استفاده از `next()`

#### 👥 مدیریت کاربران و RBAC

- ✅ **API Routes مدیریت کاربران**
  - `GET /api/admin/users` - لیست کاربران با فیلتر و جستجو
  - `GET /api/admin/users/[id]` - جزئیات کاربر
  - `PUT /api/admin/users/[id]` - ویرایش کاربر
  - `DELETE /api/admin/users/[id]` - حذف (غیرفعال‌سازی) کاربر
  - `PUT /api/admin/users/[id]/roles` - تخصیص نقش‌ها
  - `GET/POST /api/admin/users/[id]/permissions` - مدیریت مجوزهای سفارشی
- ✅ **API Routes مجوزها و نقش‌ها**
  - `GET /api/admin/permissions` - لیست مجوزها
  - `GET /api/admin/roles` - لیست نقش‌ها
- ✅ **صفحه لیست کاربران** (`/admin/users`)
  - جدول حرفه‌ای با اطلاعات کامل
  - جستجوی پیشرفته (شماره، نام، نام کاربری)
  - فیلترهای چندگانه (نقش، وضعیت، نوع کاربر)
  - Pagination
  - نمایش Avatar, Badge نقش‌ها، وضعیت
  - دکمه‌های عملیات (ویرایش، مجوزها)
  - Modal ویرایش کاربر (در حال توسعه)
  - Modal مدیریت مجوزها (در حال توسعه)
- ✅ **لینک فعال در Sidebar**
  - دسترسی سریع به صفحه کاربران

#### 🎨 طراحی مجدد فرانت‌اند

- ✅ **صفحه لاگین کاربران** (`/login`)
  - دو تب: ورود با کد یکبار مصرف / ورود با رمز عبور
  - هدایت هوشمند: کاربر جدید → `/complete-profile` / قدیمی → `/dashboard`
  - پیام قوانین و مقررات
  - لینک فراموشی رمز عبور
  - لینک ورود ادمین
- ✅ **صفحه لاگین ادمین جداگانه** (`/admin/login`)
  - دو تب: ورود با OTP / ورود با رمز
  - طراحی مینیمال و مدرن
  - انیمیشن‌های ملایم
  - مدیریت حالت‌های Loading و Error
  - Countdown تایمر برای OTP
- ✅ **کامپوننت ThemeToggle**
  - دکمه تغییر تم (روشن/تیره)
  - 5 Variant: default, floating, small, large, inline
  - انیمیشن‌های ملایم
  - پشتیبانی از Accessibility
  - ذخیره در LocalStorage
- ✅ **Layout پنل مدیریت**
  - Sidebar قابل جمع شدن
  - Navigation با آیکون و متن
  - پروفایل کاربر با دکمه خروج
  - Responsive برای موبایل و تبلت
- ✅ **داشبورد ادمین**
  - 4 کارت آماری با آیکون و رنگ منحصر
  - بخش دسترسی سریع (Quick Actions)
  - لیست فعالیت‌های اخیر
  - طراحی کارتی و مدرن
- ✅ **صفحه اصلی (`/`)**
  - Hero Section با لوگوی انیمیشن‌دار
  - بخش ویژگی‌ها (Features)
  - CTA Section با گرادیان
  - Footer کامل
- ✅ **بهبود Global Styles**
  - افزودن CSS Variables جدید
  - Utility Classes
  - انیمیشن‌های fadeIn، pulse، float
  - Focus Styles
- ✅ **اضافه شدن ThemeToggle به تمام صفحات**
  - صفحه اصلی (Floating)
  - صفحات Login (Floating)
  - پنل ادمین (Sidebar)
- ✅ **داکیومنت‌های جامع**
  - `docs/ADMIN_PANEL.md` - راهنمای پنل مدیریت
  - `docs/LOGIN_PAGES.md` - راهنمای صفحات ورود
  - `docs/THEME_SYSTEM.md` - مستندات سیستم تم
  - `docs/FRONTEND_REDESIGN.md` - خلاصه تغییرات

#### تنظیمات اولیه پروژه

- ✅ راه‌اندازی پروژه Next.js 14 با **App Router**
- ✅ تنظیمات package.json با تمام dependencies
- ✅ پیکربندی Next.js (next.config.js)
- ✅ تنظیمات ESLint
- ✅ تنظیمات jsconfig.json با Path Aliases
- ✅ فایل .browserslistrc برای سازگاری با مرورگرها

#### معماری App Router

- ✅ ساختار app/ directory به جای pages/
- ✅ Root Layout با metadata و font optimization
- ✅ Server Components و Client Components
- ✅ Built-in Loading و Error components
- ✅ صفحه 404 سفارشی (not-found.js)
- ✅ API Routes با App Router syntax

#### سیستم تایپوگرافی (Typography)

- ✅ فونت فارسی: Vazirmatn (متن)، Yekan (عناوین)
- ✅ فونت انگلیسی: Inter (متن)، Poppins (عناوین)
- ✅ بارگذاری خودکار از CDN با fallback
- ✅ Type Scale کامل (12px تا 48px)
- ✅ وزن‌های متنوع (300-800)
- ✅ تنظیمات Line Height و Letter Spacing
- ✅ پشتیبانی کامل از چندزبانه (fa/en)
- ✅ Font Feature Settings برای خوانایی بهتر

#### سیستم کاربری و RBAC

- ✅ مدل User با MongoDB/Mongoose
- ✅ احراز هویت با شماره موبایل + OTP
- ✅ پشتیبانی از رمز ثابت (اختیاری)
- ✅ نقش‌های چندگانه (Guest, User, Event Owner, Moderator, Admin)
- ✅ وضعیت‌های کاربر (Active, Verified, Suspended, etc.)
- ✅ انواع کاربر (Individual, Organization, Government)
- ✅ سیستم RBAC کامل
- ✅ مدل Permission و UserPermission
- ✅ Middleware احراز هویت و بررسی دسترسی
- ✅ سیستم لاگ فعالیت‌ها (ActivityLog)
- ✅ آمارگیری و مانیتورینگ کاربران
- ✅ پروفایل چند‌سطحی بسته به نوع کاربر

#### پنل ادمین - Frontend

- ✅ سرویس ارسال SMS با SMS.ir
- ✅ API Routes کامل برای احراز هویت (send-otp, verify-otp, login, refresh)
- ✅ AuthContext برای مدیریت Authentication
- ✅ کامپوننت‌های UI قابل استفاده مجدد (Button, Input, Card)
- ✅ صفحه Login با دو روش ورود (OTP / Password)
- ✅ Layout پنل ادمین با Sidebar و Header
- ✅ صفحه Dashboard با آمار و نمودار
- ✅ Protected Routes با بررسی نقش کاربر
- ✅ تم روشن/تیره در پنل ادمین
- ✅ Responsive Design برای موبایل

#### سیستم تم (Theme System)

- ✅ سیستم کامل تم روشن/تیره
- ✅ ThemeContext با React Context API
- ✅ فایل پیکربندی تم (theme.config.js)
- ✅ CSS Variables برای رنگ‌ها، فونت‌ها، spacing
- ✅ پشتیبانی از localStorage برای ذخیره تم
- ✅ تشخیص خودکار تم سیستم‌عامل

#### استایل‌ها

- ✅ Global CSS با تمام استایل‌های پایه
- ✅ CSS Modules برای کامپوننت‌ها
- ✅ سیستم رنگ‌بندی کامل (Primary, Secondary, Status, ...)
- ✅ Typography System (فونت، سایز، وزن، ...)
- ✅ Spacing و Sizing ماژولار
- ✅ Shadow System
- ✅ Animation و Transition
- ✅ Responsive Design
- ✅ Custom Scrollbar
- ✅ Selection Styles

#### صفحات

- ✅ صفحه اصلی (Home Page) با طراحی مدرن
- ✅ Custom \_app.js
- ✅ Custom \_document.js
- ✅ API Route برای Health Check

#### مستندات

- ✅ README.md کامل با راهنمای استفاده
- ✅ ARCHITECTURE.md - معماری پروژه
- ✅ THEME_GUIDE.md - راهنمای کامل سیستم تم
- ✅ SETUP_GUIDE.md - راهنمای راه‌اندازی
- ✅ CONTRIBUTING.md - راهنمای مشارکت
- ✅ README در هر پوشه برای توضیح ساختار

#### فایل‌های پیکربندی

- ✅ manifest.json برای PWA
- ✅ robots.txt
- ✅ .env.example
- ✅ .gitignore

#### ویژگی‌های اضافی

- ✅ پشتیبانی از چندزبانه (i18n setup برای فارسی/انگلیسی)
- ✅ SEO Meta Tags
- ✅ Open Graph Tags
- ✅ Security Headers
- ✅ Font Optimization
- ✅ Image Optimization Setup

### در حال توسعه (In Progress)

- 🔄 سیستم احراز هویت
- 🔄 اتصال به MongoDB
- 🔄 کامپوننت‌های UI مشترک

### برنامه‌ریزی شده (Planned)

- 📅 سیستم رویدادها
- 📅 نقشه تعاملی
- 📅 سیستم چت
- 📅 کیف پول و پرداخت
- 📅 داشبورد کاربری
- 📅 سیستم نوتیفیکیشن

---

## نسخه‌بندی

پروژه از [Semantic Versioning](https://semver.org/) استفاده می‌کند:

- **MAJOR**: تغییرات ناسازگار در API
- **MINOR**: افزودن قابلیت جدید (سازگار)
- **PATCH**: رفع باگ (سازگار)

## دسته‌بندی تغییرات

- **Added**: ویژگی‌های جدید
- **Changed**: تغییرات در ویژگی‌های موجود
- **Deprecated**: ویژگی‌هایی که به زودی حذف می‌شوند
- **Removed**: ویژگی‌های حذف شده
- **Fixed**: رفع باگ
- **Security**: رفع مشکلات امنیتی
