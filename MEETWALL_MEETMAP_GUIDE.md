# راهنمای صفحات MeetWall و MeetMap

این سند راهنمای کامل دو صفحه جدید **MeetWall** (نمایش لیستی) و **MeetMap** (نمایش نقشه‌ای) رویدادها را ارائه می‌دهد.

## 📋 فهرست مطالب
- [معرفی](#معرفی)
- [ساختار فایل‌ها](#ساختار-فایلها)
- [MeetWall - نمایش لیستی](#meetwall---نمایش-لیستی)
- [MeetMap - نمایش نقشه‌ای](#meetmap---نمایش-نقشهای)
- [ویژگی‌های مشترک](#ویژگیهای-مشترک)
- [نصب و راه‌اندازی](#نصب-و-راهاندازی)
- [توسعه و سفارشی‌سازی](#توسعه-و-سفارشیسازی)

---

## معرفی

این دو صفحه برای نمایش رویدادهای تایید شده سیستم طراحی شده‌اند و به کاربران امکان می‌دهند رویدادها را به دو روش مختلف مشاهده کنند:

1. **MeetWall**: نمایش لیستی با فیلترهای پیشرفته
2. **MeetMap**: نمایش روی نقشه تعاملی

هر دو صفحه شامل:
- ✅ تم روشن و تاریک
- ✅ طراحی ریسپانسیو
- ✅ فیلترهای هوشمند
- ✅ تعامل بالا با کاربر
- ✅ بهینه‌سازی عملکرد

---

## ساختار فایل‌ها

```
src/app/
├── meetwall/
│   ├── page.js              # کامپوننت اصلی صفحه لیستی
│   ├── meetwall.module.css  # استایل‌های تم روشن
│   ├── meetwallDark.css     # استایل‌های تم تاریک
│   └── README.md            # راهنمای صفحه
│
└── meetmap/
    ├── page.js              # کامپوننت اصلی صفحه نقشه
    ├── MapContainer.js      # کامپوننت نقشه (Leaflet)
    ├── meetmap.module.css   # استایل‌های تم روشن
    ├── meetmapDark.css      # استایل‌های تم تاریک
    └── README.md            # راهنمای صفحه
```

---

## MeetWall - نمایش لیستی

### مسیر دسترسی
```
/meetwall
```

### ویژگی‌های کلیدی

#### 1. فیلترهای پیشرفته
- 🔍 **جستجو**: جستجوی رویداد بر اساس نام
- 💰 **بازه قیمت**: انتخاب حداقل و حداکثر با اسلایدر
- ⭐ **ستاره**: فیلتر بر اساس امتیاز (۱-۵ ستاره)
- 📊 **دسته‌بندی**: فیلتر موضوعی
- 📍 **نوع برگزاری**: حضوری/آنلاین/ترکیبی
- 📅 **تاریخ**: انتخاب تاریخ شروع
- 🌙 **تعداد تخت**: فیلتر ظرفیت

#### 2. مرتب‌سازی
- کمترین قیمت
- بیشترین قیمت
- بالاترین امتیاز

#### 3. نمایش رویدادها
هر کارت رویداد شامل:
- تصویر با افکت hover
- عنوان و امتیاز
- موقعیت مکانی
- توضیحات مختصر
- دسته‌بندی‌ها
- قیمت (با نمایش تخفیف)
- دکمه مشاهده جزئیات

#### 4. صفحه‌بندی
- ۱۲ رویداد در هر صفحه
- دکمه‌های قبلی/بعدی
- نمایش شماره صفحات

### مثال استفاده در کد

```javascript
// دریافت رویدادها با فیلتر
const response = await fetch(`/api/events?${queryParams}`, {
  credentials: "include",
});

// فیلترهای قابل استفاده:
// - status: "approved"
// - page: شماره صفحه
// - limit: تعداد در هر صفحه
// - search: جستجو در عنوان
// - category: شناسه دسته‌بندی
// - formatMode: نوع برگزاری
// - city: شهر
// - province: استان
// - dateFrom: تاریخ شروع از
// - dateTo: تاریخ شروع تا
```

---

## MeetMap - نمایش نقشه‌ای

### مسیر دسترسی
```
/meetmap
```

### ویژگی‌های کلیدی

#### 1. نقشه تعاملی
- 🗺️ نقشه کامل با Leaflet
- 📍 مارکرهای سفارشی برای هر رویداد
- 🟢 مارکر سبز: رویداد عادی
- 🔴 مارکر قرمز: رویداد انتخاب شده
- 💬 پاپ‌آپ اطلاعات برای هر مارکر

#### 2. کنترل‌های نقشه
- **+**: بزرگنمایی
- **-**: کوچکنمایی
- **🎯**: بازگشت به مرکز (مشهد)

#### 3. سایدبار رویدادها
- لیست کامل رویدادها
- کارت‌های کوچک با اطلاعات کلیدی
- اسکرول عمودی
- هایلایت رویداد انتخاب شده

#### 4. تعامل دوطرفه
- کلیک روی مارکر → نمایش در لیست
- کلیک روی کارت → تمرکز روی نقشه
- همگام‌سازی خودکار

#### 5. Legend
نمایش راهنمای مارکرها در پایین نقشه

### مثال استفاده MapContainer

```javascript
import MapContainer from "./MapContainer";

<MapContainer
  events={events}           // آرایه رویدادها
  center={[36.2972, 59.6067]} // مرکز نقشه [lat, lng]
  zoom={12}                 // سطح زوم
  onMarkerClick={handleMarkerClick} // هندلر کلیک مارکر
  selectedEvent={selectedEvent}     // رویداد انتخاب شده
/>
```

---

## ویژگی‌های مشترک

### 1. تم روشن و تاریک

هر دو صفحه از سیستم تم مدیریت شده استفاده می‌کنند:

```javascript
import { loadInitialTheme, toggleTheme } from "@/lib/utils/themeManager";

// بارگذاری تم اولیه
const initialTheme = loadInitialTheme();
setDarkMode(initialTheme === 'dark');

// تغییر تم
const toggleDarkMode = () => {
  const currentTheme = darkMode ? 'dark' : 'light';
  const newTheme = toggleTheme(currentTheme);
  setDarkMode(newTheme === 'dark');
};
```

**استایل‌های تم تاریک**:
- فایل‌های `*Dark.css` به صورت خودکار بارگذاری می‌شوند
- از سلکتور `[data-theme="dark"]` استفاده می‌کنند
- تمام رنگ‌ها و سایه‌ها بهینه شده‌اند

### 2. طراحی ریسپانسیو

هر دو صفحه در سه سایز اصلی بهینه شده‌اند:

#### 🖥️ Desktop (> 1200px)
- MeetWall: فیلتر سمت راست (۳۵۰px) + لیست مرکز
- MeetMap: نقشه سمت چپ (۷۰٪) + سایدبار سمت راست (۳۰٪)

#### 📱 Tablet (768px - 1200px)
- MeetWall: فیلتر بالا + لیست پایین
- MeetMap: نقشه بالا + سایدبار پایین

#### 📱 Mobile (< 768px)
- هر دو: نمایش عمودی با یک ستون
- فونت‌ها و دکمه‌ها بزرگتر
- فضای بیشتر برای لمس

### 3. مدیریت خطا

```javascript
if (error) {
  return (
    <div className={styles.error}>
      <span>❌</span>
      <p>{error}</p>
      <button onClick={fetchEvents}>تلاش مجدد</button>
    </div>
  );
}
```

### 4. Loading State

```javascript
if (loading && events.length === 0) {
  return (
    <div className={styles.loading}>
      <div className={styles.spinner}></div>
      <p>در حال بارگذاری...</p>
    </div>
  );
}
```

---

## نصب و راه‌اندازی

### پیش‌نیازها

کتابخانه‌های مورد نیاز قبلاً در `package.json` موجود است:

```json
{
  "leaflet": "^1.9.4",
  "react-leaflet": "^4.2.1"
}
```

اگر نیاز به نصب مجدد دارید:

```bash
npm install leaflet react-leaflet
```

### راه‌اندازی

1. **اجرای سرور توسعه**:
```bash
npm run dev
```

2. **دسترسی به صفحات**:
- MeetWall: http://localhost:3000/meetwall
- MeetMap: http://localhost:3000/meetmap

3. **بررسی API**:
اطمینان حاصل کنید که API رویدادها در مسیر زیر فعال است:
```
GET /api/events?status=approved
```

---

## توسعه و سفارشی‌سازی

### 1. تغییر تعداد رویدادها در هر صفحه

**MeetWall**:
```javascript
// در page.js
const limit = parseInt(searchParams.get('limit')) || 12; // تغییر به عدد دلخواه
```

**MeetMap**:
```javascript
// در page.js
limit: 100, // تغییر به عدد دلخواه
```

### 2. افزودن فیلتر جدید

```javascript
// 1. اضافه کردن به state
const [filters, setFilters] = useState({
  ...
  newFilter: "", // فیلتر جدید
});

// 2. اضافه کردن UI
<div className={styles.filterSection}>
  <label>فیلتر جدید</label>
  <select
    value={filters.newFilter}
    onChange={(e) => handleFilterChange('newFilter', e.target.value)}
  >
    <option value="">همه</option>
    {/* گزینه‌ها */}
  </select>
</div>

// 3. ارسال به API
const queryParams = new URLSearchParams({
  ...
  ...(filters.newFilter && { newFilter: filters.newFilter }),
});
```

### 3. سفارشی‌سازی مارکرهای نقشه

در `MapContainer.js`:

```javascript
const icon = L.divIcon({
  className: 'custom-marker',
  html: `
    <div style="
      background: ${isSelected ? '#ef4444' : '#10b981'};
      // استایل‌های سفارشی
    ">
      <span>📍</span>
    </div>
  `,
  iconSize: [32, 32],
  iconAnchor: [16, 32],
});
```

### 4. تغییر رنگ‌های تم

**تم روشن**:
```css
/* در meetwall.module.css یا meetmap.module.css */
.container {
  background: linear-gradient(135deg, #YOUR_COLOR 0%, #YOUR_COLOR 100%);
}

.title {
  color: #YOUR_COLOR;
}
```

**تم تاریک**:
```css
/* در meetwallDark.css یا meetmapDark.css */
[data-theme="dark"] .meetWallPage .container {
  background: linear-gradient(135deg, #YOUR_COLOR 0%, #YOUR_COLOR 100%);
}
```

### 5. افزودن مرتب‌سازی جدید

```javascript
// در MeetWall/page.js
const sortOptions = {
  priceAsc: 'کمترین قیمت',
  priceDesc: 'بیشترین قیمت',
  ratingDesc: 'بالاترین امتیاز',
  dateAsc: 'جدیدترین', // جدید
};

// ارسال به API
const queryParams = new URLSearchParams({
  ...
  sort: selectedSort,
});
```

---

## نکات مهم برای توسعه‌دهندگان

### 1. SSR و Next.js
- کامپوننت نقشه با `dynamic import` و `ssr: false` بارگذاری می‌شود
- این به دلیل استفاده از `window` و `document` در Leaflet است

### 2. Performance
- تصاویر با `onError` fallback دارند
- فیلترها با `debounce` (اختیاری) بهینه می‌شوند
- از `useState` و `useEffect` به‌درستی استفاده شده

### 3. Accessibility
- تمام دکمه‌ها دارای `title` هستند
- رنگ‌ها کنتراست مناسب دارند
- از semantic HTML استفاده شده

### 4. SEO
- عناوین مناسب با `h1`, `h2`
- alt text برای تصاویر
- meta tags (قابل افزودن در layout.js)

---

## مشکلات متداول و راه‌حل‌ها

### مشکل: نقشه نمایش داده نمی‌شود
**راه‌حل**: 
1. بررسی console browser برای خطاهای CSS
2. اطمینان از نصب صحیح leaflet
3. بررسی مسیر فایل‌های CSS leaflet

### مشکل: مارکرها روی نقشه نیستند
**راه‌حل**:
1. بررسی `location.coordinates` در دیتای رویدادها
2. اطمینان از فرمت صحیح: `[longitude, latitude]`
3. چک کردن اینکه رویدادها داده‌های معتبر دارند

### مشکل: فیلترها کار نمی‌کنند
**راه‌حل**:
1. بررسی API endpoint
2. console.log کردن queryParams
3. بررسی response از API

### مشکل: تم تاریک اعمال نمی‌شود
**راه‌حل**:
1. بررسی import فایل `*Dark.css`
2. بررسی attribute `data-theme` در HTML
3. بررسی localStorage برای ذخیره تم

---

## تست و کیفیت

### تست‌های توصیه‌شده

#### 1. تست عملکردی
- [ ] نمایش صحیح رویدادها
- [ ] عملکرد فیلترها
- [ ] صفحه‌بندی
- [ ] تغییر تم
- [ ] انتقال بین دو صفحه

#### 2. تست ریسپانسیو
- [ ] Desktop (1920x1080)
- [ ] Laptop (1366x768)
- [ ] Tablet (768x1024)
- [ ] Mobile (375x667)

#### 3. تست مرورگرها
- [ ] Chrome/Edge
- [ ] Firefox
- [ ] Safari
- [ ] Mobile Browsers

#### 4. تست عملکرد
- [ ] سرعت بارگذاری
- [ ] حجم دانلود
- [ ] تعداد درخواست‌ها
- [ ] مصرف حافظه

---

## پشتیبانی و مستندات بیشتر

برای اطلاعات بیشتر به فایل‌های README در هر پوشه مراجعه کنید:
- `src/app/meetwall/README.md`
- `src/app/meetmap/README.md`

---

## نسخه و تاریخچه

- **v1.0.0** (۱۴۰۴/۰۸/۲۱): انتشار اولیه
  - صفحه MeetWall
  - صفحه MeetMap
  - تم روشن و تاریک
  - فیلترهای پیشرفته
  - طراحی ریسپانسیو

---

## لایسنس و حق نشر

این پروژه تحت لایسنس PlusMeet است.
© ۱۴۰۴ - تمامی حقوق محفوظ است.

---

**ساخته شده با ❤️ برای PlusMeet**

