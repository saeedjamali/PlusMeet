# صفحه MeetMap - نمایش نقشه‌ای رویدادها

این صفحه برای نمایش رویدادها روی نقشه با امکان تعامل طراحی شده است.

## ویژگی‌ها

### 1. **نقشه تعاملی**
- استفاده از کتابخانه Leaflet برای نمایش نقشه
- نمایش تمام رویدادها به صورت مارکر روی نقشه
- امکان زوم و حرکت روی نقشه
- مارکرهای سفارشی با رنگ‌های متفاوت:
  - 🟢 سبز: رویدادهای عادی
  - 🔴 قرمز: رویداد انتخاب شده

### 2. **لیست رویدادها در سایدبار**
سایدبار سمت راست شامل:
- فیلترهای سریع (جستجو، ستاره، دسته‌بندی)
- لیست کامل رویدادها به صورت کارت‌های کوچک
- امکان اسکرول و مشاهده همه رویدادها
- هایلایت کردن رویداد انتخاب شده

### 3. **تعامل بین نقشه و لیست**
- کلیک روی مارکر: انتخاب رویداد و نمایش در سایدبار
- کلیک روی کارت رویداد: تمرکز نقشه روی آن رویداد
- نمایش پاپ‌آپ اطلاعات رویداد روی نقشه

### 4. **کنترل‌های نقشه**
- **+**: بزرگنمایی نقشه
- **-**: کوچکنمایی نقشه
- **🎯**: بازگشت به مرکز نقشه (مشهد)

### 5. **فیلترهای سریع**
- جستجوی نام رویداد
- فیلتر بر اساس ستاره
- فیلتر بر اساس دسته‌بندی

### 6. **Legend نقشه**
نمایش راهنمای مارکرها در پایین نقشه

### 7. **تم روشن و تاریک**
- پشتیبانی کامل از تم روشن و تاریک
- دکمه تغییر تم در هدر صفحه
- استایل‌های سفارشی برای نقشه در حالت تاریک

### 8. **تغییر نمایش**
- دکمه تغییر به نمایش لیستی (MeetWall)
- لینک مستقیم به صفحه لیست

### 9. **Toolbar شناور**
دو دکمه در پایین صفحه:
- لیست هتل‌های مشهد
- تغییر نمایش

## مسیر دسترسی
```
/meetmap
```

## API مورد استفاده
این صفحه از API زیر برای دریافت رویدادها استفاده می‌کند:
```
GET /api/events?status=approved&limit=100&...filters
```

**توجه**: برای نمایش روی نقشه، محدودیت تعداد بیشتر است (۱۰۰ رویداد).

## فایل‌های مرتبط
- `page.js` - کامپوننت اصلی صفحه
- `MapContainer.js` - کامپوننت نقشه
- `meetmap.module.css` - استایل‌های تم روشن
- `meetmapDark.css` - استایل‌های تم تاریک

## کتابخانه‌های مورد استفاده
```json
{
  "leaflet": "^1.9.4",
  "react-leaflet": "^4.2.1"
}
```

## نکات مهم
1. فقط رویدادهایی که دارای `location.coordinates` هستند روی نقشه نمایش داده می‌شوند
2. نقشه به صورت پویا (Dynamic Import) بارگذاری می‌شود تا از SSR جلوگیری شود
3. مرکز پیش‌فرض نقشه: مشهد (۳۶.۲۹۷۲, ۵۹.۶۰۶۷)
4. زوم پیش‌فرض: ۱۲
5. هنگام انتخاب رویداد، زوم به ۱۵ تغییر می‌کند

## رسپانسیو
- **دسکتاپ**: نقشه در سمت چپ (۷۰٪) + سایدبار در سمت راست (۳۰٪)
- **تبلت**: نقشه بالا + سایدبار پایین
- **موبایل**: نمایش به صورت عمودی

## عملکرد مارکرها
هر مارکر شامل:
- آیکون سفارشی به شکل پین
- رنگ متفاوت برای حالت عادی و انتخاب شده
- پاپ‌آپ با اطلاعات رویداد:
  - عنوان
  - موقعیت
  - قیمت
  - دکمه "مشاهده جزئیات"

## بهبودهای آینده (پیشنهادی)
- [ ] اضافه کردن Cluster برای مارکرهای نزدیک به هم
- [ ] فیلتر بر اساس محدوده نمایش نقشه
- [ ] نمایش مسیر بین رویدادها
- [ ] اضافه کردن لایه‌های مختلف نقشه (ماهواره، خیابان و...)
- [ ] دکمه "موقعیت من" برای تمرکز بر موقعیت کاربر

