# 👥 مدیریت کاربران PlusMeet

## 📖 نمای کلی

سیستم مدیریت کاربران PlusMeet ابزاری جامع برای مدیریت، جستجو، فیلتر و تخصیص نقش‌ها و مجوزها به کاربران است.

---

## 🎯 دسترسی

### مسیر

```
/admin/users
```

### نقش‌های مجاز

- **Admin**: دسترسی کامل
- **Moderator**: فقط مشاهده و جستجو

---

## 🔍 قابلیت‌های صفحه

### 1. لیست کاربران

**نمایش اطلاعات:**

- Avatar کاربر
- نام و نام کاربری
- شماره موبایل
- ایمیل (اختیاری)
- نقش‌ها (Badges رنگی)
- وضعیت (Badge)
- نوع کاربر
- تاریخ عضویت

### 2. جستجو

جستجو بر اساس:

- شماره موبایل
- نام
- نام خانوادگی
- نام کاربری

**مثال:**

```
09123456789
```

### 3. فیلترها

**فیلتر بر اساس نقش:**

- همه نقش‌ها
- مدیر
- ناظر
- مالک رویداد
- کاربر

**فیلتر بر اساس وضعیت:**

- همه وضعیت‌ها
- فعال
- تایید شده
- معلق
- حذف شده

**فیلتر بر اساس نوع کاربر:**

- همه انواع
- فردی
- سازمانی
- دولتی

### 4. Pagination

- 20 کاربر در هر صفحه
- نمایش تعداد کل کاربران
- دکمه‌های قبلی/بعدی
- نمایش شماره صفحه فعلی

---

## 🎨 رنگ‌بندی Badge ها

### نقش‌ها

| نقش         | رنگ      | Hex       |
| ----------- | -------- | --------- |
| مدیر        | قرمز     | `#E5644F` |
| ناظر        | آبی تیره | `#267D88` |
| مالک رویداد | نارنجی   | `#F4A325` |
| کاربر       | سبز      | `#3DA672` |

### وضعیت‌ها

| وضعیت     | رنگ     | Hex       |
| --------- | ------- | --------- |
| فعال      | سبز     | `#3DA672` |
| تایید شده | آبی     | `#267D88` |
| معلق      | زرد     | `#E2981E` |
| حذف شده   | خاکستری | `#A39B8F` |

---

## ⚙️ عملیات روی کاربران

### 1. ویرایش کاربر

**دسترسی:** فقط Admin

**فیلدهای قابل ویرایش:**

- نام
- نام خانوادگی
- نام کاربری (displayName)
- ایمیل
- وضعیت
- نقش‌ها
- نوع کاربر
- پروفایل
- تنظیمات
- حریم خصوصی

**API Endpoint:**

```
PUT /api/admin/users/[phoneNumber]
```

**نمونه Request:**

```json
{
  "firstName": "علی",
  "lastName": "احمدی",
  "displayName": "علی احمدی",
  "email": "ali@example.com",
  "status": "verified",
  "roles": ["user", "event_owner"]
}
```

### 2. تخصیص نقش‌ها

**دسترسی:** فقط Admin

**نقش‌های موجود:**

- `guest` - مهمان
- `user` - کاربر
- `event_owner` - مالک رویداد
- `moderator` - ناظر
- `admin` - مدیر

**نکته مهم:** ⚠️  
مدیر نمی‌تواند نقش Admin را از خودش حذف کند.

**API Endpoint:**

```
PUT /api/admin/users/[phoneNumber]/roles
```

**نمونه Request:**

```json
{
  "roles": ["user", "event_owner", "moderator"]
}
```

### 3. مدیریت مجوزهای سفارشی

**دسترسی:** فقط Admin

**مجوزهای سفارشی:**

- مجوزهای موقت با تاریخ انقضا
- مجوزهای محدود به Scope خاص
- یادداشت برای هر مجوز

**دریافت مجوزهای کاربر:**

```
GET /api/admin/users/[phoneNumber]/permissions
```

**تخصیص مجوز جدید:**

```
POST /api/admin/users/[phoneNumber]/permissions
```

**نمونه Request:**

```json
{
  "permissionId": "60d5f...",
  "expiresAt": "2025-12-31T23:59:59.000Z",
  "scope": {
    "city": "Tehran"
  },
  "notes": "مجوز موقت برای مدیریت رویدادهای تهران"
}
```

### 4. حذف (غیرفعال‌سازی) کاربر

**دسترسی:** فقط Admin

**نکته:** ⚠️  
کاربر به صورت فیزیکی حذف نمی‌شود، فقط وضعیت آن به `deleted` تغییر می‌کند.

**API Endpoint:**

```
DELETE /api/admin/users/[phoneNumber]
```

---

## 📊 API Endpoints

### لیست کاربران

```
GET /api/admin/users
```

**Query Parameters:**

| پارامتر     | نوع    | پیش‌فرض   | توضیح         |
| ----------- | ------ | --------- | ------------- |
| `page`      | number | 1         | شماره صفحه    |
| `limit`     | number | 20        | تعداد در صفحه |
| `search`    | string | -         | جستجو         |
| `role`      | string | -         | فیلتر نقش     |
| `status`    | string | -         | فیلتر وضعیت   |
| `userType`  | string | -         | فیلتر نوع     |
| `sortBy`    | string | createdAt | مرتب‌سازی     |
| `sortOrder` | string | desc      | نوع مرتب‌سازی |

**نمونه Response:**

```json
{
  "success": true,
  "data": {
    "users": [...],
    "pagination": {
      "page": 1,
      "limit": 20,
      "total": 150,
      "totalPages": 8,
      "hasNextPage": true,
      "hasPrevPage": false
    }
  }
}
```

### جزئیات کاربر

```
GET /api/admin/users/[phoneNumber]
```

**نمونه Response:**

```json
{
  "success": true,
  "data": {
    "_id": "...",
    "phoneNumber": "09123456789",
    "firstName": "علی",
    "lastName": "احمدی",
    "displayName": "علی احمدی",
    "email": "ali@example.com",
    "roles": ["user", "event_owner"],
    "status": "verified",
    "userType": "individual",
    "createdAt": "2025-01-01T00:00:00.000Z",
    ...
  }
}
```

### لیست مجوزها

```
GET /api/admin/permissions
```

**Query Parameters:**

- `category` (optional): فیلتر بر اساس دسته‌بندی

**نمونه Response:**

```json
{
  "success": true,
  "data": {
    "permissions": [...],
    "grouped": {
      "events": [...],
      "users": [...],
      "reports": [...]
    }
  }
}
```

### لیست نقش‌ها

```
GET /api/admin/roles
```

**نمونه Response:**

```json
{
  "success": true,
  "data": [
    {
      "_id": "...",
      "name": "admin",
      "description": "مدیر کل سیستم",
      "permissions": [...]
    },
    ...
  ]
}
```

---

## 🔐 امنیت

### Authentication

تمام API endpoints نیاز به Authentication دارند:

```javascript
headers: {
  'Authorization': 'Bearer YOUR_ACCESS_TOKEN'
}
```

### Authorization

- **مشاهده لیست:** Admin, Moderator
- **ویرایش:** فقط Admin
- **تخصیص نقش:** فقط Admin
- **تخصیص مجوز:** فقط Admin
- **حذف:** فقط Admin

### Activity Logging

تمام عملیات در ActivityLog ثبت می‌شوند:

- نوع عملیات
- کاربر انجام‌دهنده
- کاربر هدف
- زمان
- IP Address
- User Agent

---

## 💡 نکات و Tips

### 1. جستجوی سریع

برای جستجوی سریع، شماره موبایل را بدون `+98` یا `0` وارد کنید:

```
✅ 9123456789
✅ 09123456789
❌ +989123456789
```

### 2. فیلترهای ترکیبی

می‌توانید چند فیلتر را همزمان استفاده کنید:

```
نقش: مالک رویداد
وضعیت: تایید شده
نوع: سازمانی
```

### 3. Export داده‌ها (آینده)

در نسخه آینده امکان Export لیست کاربران به فرمت‌های مختلف اضافه خواهد شد:

- CSV
- Excel
- PDF

### 4. Bulk Operations (آینده)

- تخصیص نقش دسته‌جمعی
- تغییر وضعیت دسته‌جمعی
- ارسال پیام گروهی

---

## 🎨 UI Components

### User Avatar

```jsx
<div className={styles.userAvatar}>{user.displayName?.[0] || "U"}</div>
```

### Badge Component

```jsx
<span className={`${styles.badge} ${styles.badgeAdmin}`}>مدیر</span>
```

### Action Buttons

```jsx
<button className={styles.actionBtn} title="ویرایش">
  <svg>...</svg>
</button>
```

---

## 🐛 Troubleshooting

### لیست کاربران نمایش داده نمی‌شود

```bash
1. بررسی MongoDB Connection
2. بررسی Authentication Token
3. بررسی console برای خطا
```

### جستجو کار نمی‌کند

```bash
1. حداقل 3 کاراکتر وارد کنید
2. از فارسی یا انگلیسی استفاده کنید
3. برای شماره، فقط اعداد وارد کنید
```

### فیلترها اعمال نمی‌شوند

```bash
1. صفحه را Refresh کنید
2. روی دکمه "جستجو" کلیک کنید
3. Cache مرورگر را پاک کنید
```

---

## 🚀 توسعه آینده

- [ ] مودال ویرایش کامل با فرم
- [ ] مودال مدیریت مجوزها با لیست
- [ ] Export به CSV/Excel
- [ ] Bulk Operations
- [ ] نمایش تاریخچه فعالیت‌های کاربر
- [ ] گزارش‌گیری پیشرفته
- [ ] نمودار آمار کاربران

---

## 📞 پشتیبانی

برای اطلاعات بیشتر:

- [سیستم کاربری](USER_SYSTEM.md)
- [RBAC](RBAC_GUIDE.md)
- [پنل مدیریت](ADMIN_PANEL.md)

---

**تاریخ آخرین به‌روزرسانی:** 27 اکتبر 2025



