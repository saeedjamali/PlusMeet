# 🗂️ مدیریت دسترسی منوها (Menu Permissions)

این سند نحوه کار سیستم نمایش دینامیک منوها براساس مجوزات کاربران را توضیح می‌دهد.

---

## 📋 خلاصه

در PlusMeet، منوهای Sidebar پنل ادمین براساس **نقش‌های کاربر** نمایش داده می‌شوند:

- ✅ **Admin**: همه منوها را می‌بیند
- ✅ **Event Owner**: فقط منوهای مرتبط با رویدادها
- ✅ **Moderator**: منوهای مدیریت محتوا
- ✅ **User**: دسترسی محدود

**مزیت اصلی:** تغییرات مجوزات بدون نیاز به تغییر کد یا restart سرور اعمال می‌شوند!

---

## 🎯 نقش پایه `user`

### چرا نقش `user` اجباری است؟

نقش `user` به عنوان **نقش پایه** برای تمام کاربران در نظر گرفته شده است:

```
همه کاربران = user + نقش‌های دیگر

مثال‌ها:
- کاربر عادی: ["user"]
- مالک رویداد: ["user", "event_owner"]
- مدیر: ["user", "admin"]
```

### چگونه تضمین می‌شود؟

#### 1. در API Route تغییر نقش

```javascript
// src/app/api/admin/users/[id]/roles/route.js

// اطمینان از وجود نقش پایه 'user' برای همه کاربران
if (!roles.includes("user")) {
  roles.unshift("user"); // اضافه کردن 'user' به ابتدای آرایه
  console.log("✅ نقش 'user' به صورت خودکار اضافه شد");
}
```

**نتیجه:** حتی اگر Admin نقش `user` را حذف کند، خودکار دوباره اضافه می‌شود.

#### 2. در RolesModal (UI)

نقش `user` با ویژگی‌های زیر نمایش داده می‌شود:

```javascript
const isBaseRole = role.name === "user"; // نقش پایه
const isDisabled = isBaseRole;
```

- ✅ **Checkbox disabled**: غیرقابل تغییر
- ✅ **Badge "پایه"**: نمایش بصری
- ✅ **Tooltip**: "نقش پایه - همه کاربران باید این نقش را داشته باشند"
- ✅ **Opacity کمتر**: نشان دهنده غیرفعال بودن
- ✅ **Cursor `not-allowed`**: نشانگر موس تغییر می‌کند

---

## 🗂️ نمایش منوها براساس مجوزات

### معماری

```
┌─────────────────────┐
│  کاربر با نقش‌ها   │
│  roles: [...]       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  /api/user/menus    │ ← API Endpoint
│                     │
│  getAllowedMenus()  │ ← Middleware
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Query در DB:      │
│  Role.find({        │
│    slug: {          │
│      $in: roles     │
│    }                │
│  })                 │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  فیلتر منوها:      │
│  - dashboard        │
│  - users.list       │
│  - events.list      │
│  ...                │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Admin Layout       │
│  (Sidebar)          │
│                     │
│  menuItems.map()    │
└─────────────────────┘
```

---

## 🔧 API Endpoint: `/api/user/menus`

### درخواست (Request)

```http
GET /api/user/menus HTTP/1.1
Cookie: accessToken=...
```

### پاسخ (Response)

```json
{
  "success": true,
  "menus": [
    {
      "menuId": "dashboard",
      "title": "داشبورد",
      "path": "/admin",
      "icon": "📊",
      "access": "view"
    },
    {
      "menuId": "users.list",
      "title": "لیست کاربران",
      "path": "/admin/users",
      "parentId": "users",
      "access": "full"
    }
  ]
}
```

### سطوح دسترسی

- **`view`**: فقط مشاهده منو (اما ممکن است دسترسی کامل به صفحه را نداشته باشد)
- **`full`**: دسترسی کامل (مشاهده + عملیات)

---

## 🎨 فیلتر خودکار Sidebar

### کد در Admin Layout

```javascript
// src/app/admin/layout.js

// Fetch allowed menus
useEffect(() => {
  const fetchAllowedMenus = async () => {
    if (!isAuthenticated || !user) return;

    const response = await fetchWithAuth("/api/user/menus");
    const data = await response.json();

    if (data.success && data.menus) {
      // استخراج menuId های مجاز
      const menuIds = data.menus.map((menu) => menu.menuId);
      setAllowedMenuIds(menuIds);
    }
  };

  if (isAuthenticated && !authLoading) {
    fetchAllowedMenus();
  }
}, [isAuthenticated, user, authLoading, fetchWithAuth]);

// فیلتر کردن منوها براساس دسترسی
const isAdmin = user?.roles?.includes("admin");
const menuItems = isAdmin
  ? allMenuItems
  : allMenuItems.filter((item) => allowedMenuIds.includes(item.menuId));
```

### مثال عملی

**کاربر با نقش `event_owner`:**

```javascript
// نقش‌ها:
roles: ["user", "event_owner"]

// منوهای مجاز (از دیتابیس):
allowedMenuIds: ["dashboard", "events", "events.list", "events.create"]

// منوهای نمایش داده شده:
- داشبورد (/admin)
- رویدادها (/admin/events)
```

**کاربر با نقش `admin`:**

```javascript
// نقش‌ها:
roles: ["user", "admin"]

// منوهای نمایش داده شده:
همه منوها (بدون فیلتر)
```

---

## 🛠️ نحوه اضافه کردن منوی جدید

### گام 1: اضافه کردن به Seed

در `src/app/api/admin/rbac/seed/route.js`:

```javascript
const menus = [
  // منوهای موجود...
  {
    menuId: "analytics", // ← شناسه یکتا
    title: "تحلیل و آمار",
    titleEn: "Analytics",
    path: "/admin/analytics",
    icon: "📈",
    order: 5,
    defaultRoles: ["admin", "moderator"], // ← نقش‌های پیش‌فرض
  },
];
```

### گام 2: Seed کردن

```bash
# از Terminal:
npm run seed-rbac

# یا از پنل ادمین:
http://localhost:3000/admin/rbac/seed
```

### گام 3: اضافه کردن به Admin Layout

در `src/app/admin/layout.js`:

```javascript
const allMenuItems = [
  // منوهای موجود...
  {
    menuId: "analytics", // ← همان شناسه یکتا
    title: "تحلیل و آمار",
    icon: <AnalyticsIcon />,
    href: "/admin/analytics",
    active: pathname.startsWith("/admin/analytics"),
  },
];
```

### گام 4: اضافه کردن دسترسی به نقش

از صفحه ویرایش نقش (`/admin/rbac/roles/:id`):

1. تب "دسترسی منوها" را باز کنید
2. منوی جدید را پیدا کنید
3. سطح دسترسی را انتخاب کنید (👁️ مشاهده یا 🔒 کامل)
4. ذخیره کنید

**بدون restart سرور، تغییرات اعمال می‌شوند!** ✨

---

## 🔍 Debugging

### چک کردن منوهای مجاز

در Console مرورگر:

```javascript
// در Admin Layout
console.log("Allowed Menu IDs:", allowedMenuIds);
console.log("Filtered Menu Items:", menuItems);
```

### چک کردن Response API

```bash
curl -X GET http://localhost:3000/api/user/menus \
  -H "Cookie: accessToken=YOUR_TOKEN"
```

### مشکلات رایج

#### ❌ منویی نمایش داده نمی‌شود

**علت‌های احتمالی:**

1. **`menuId` اشتباه است**

   ```javascript
   // ❌ اشتباه:
   {
     menuId: "users";
   } // در allMenuItems
   // منو در seed: "users.list"

   // ✅ صحیح:
   {
     menuId: "users.list";
   }
   ```

2. **نقش کاربر دسترسی ندارد**

   - به `/admin/rbac/roles` بروید
   - نقش کاربر را ویرایش کنید
   - منو را به `menuPermissions` اضافه کنید

3. **Seed نشده است**
   ```bash
   npm run seed-rbac
   ```

#### ❌ همه منوها نمایش داده می‌شوند (حتی برای non-admin)

**علت:**

- `isAdmin` بررسی درست کار نمی‌کند
- احتمالاً `user.roles` undefined است

**راه‌حل:**

```javascript
console.log("User roles:", user?.roles);
console.log("Is Admin:", isAdmin);
```

---

## 📚 منابع مرتبط

- [DEFAULT_ROLES.md](./DEFAULT_ROLES.md) - نقش‌های پیش‌فرض و دسترسی‌هایشان
- [RBAC_GUIDE.md](./RBAC_GUIDE.md) - راهنمای کامل RBAC
- [SEEDING.md](./SEEDING.md) - راهنمای Seed کردن داده‌ها

---

**تاریخ به‌روزرسانی:** 29 اکتبر 2025  
**نسخه:** 1.0.0


