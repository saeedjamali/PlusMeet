# 📂 مستندات سیستم مدیریت دسته‌بندی موضوعات (Topic Categories)

## 📋 فهرست مطالب
- [معرفی](#معرفی)
- [ویژگی‌ها](#ویژگی‌ها)
- [ساختار دیتابیس](#ساختار-دیتابیس)
- [API Endpoints](#api-endpoints)
- [رابط کاربری](#رابط-کاربری)
- [نحوه استفاده](#نحوه-استفاده)
- [آپلود از Excel](#آپلود-از-excel)
- [نمونه کدها](#نمونه-کدها)

---

## معرفی

سیستم مدیریت دسته‌بندی موضوعات یک سیستم **چند سطحی** و **انعطاف‌پذیر** برای دسته‌بندی رویدادها براساس موضوع و محور محتوا است.

### مسیر دسترسی
```
/dashboard/cat_topic
```

### دسترسی‌های لازم
- نقش: `admin` یا `moderator`
- Permission: `topic_categories` با actions: `create`, `read`, `update`, `delete`

---

## ویژگی‌ها

### ✨ ویژگی‌های اصلی

#### 1. **سلسله مراتب چند سطحی**
- تعداد سطوح نامحدود
- قابلیت ایجاد زیردسته برای هر دسته
- محاسبه خودکار level براساس parent
- جلوگیری از ایجاد حلقه (circular reference)

#### 2. **ظاهر و نمایش**
- **آیکن**: انتخاب emoji برای هر دسته
- **رنگ پایه**: انتخاب رنگ مشخصه (HEX)
- **گرادیانت**: تعریف گرادیانت با دو رنگ + جهت
  - جهت‌های ممکن: `to-right`, `to-left`, `to-top`, `to-bottom`, `to-top-right`, `to-bottom-right`
- **پیش‌نمایش زنده**: مشاهده نمایش نهایی قبل از ذخیره

#### 3. **حس و کاربرد (Mood & Usage)**
- تعریف mood برای هر دسته:
  - احساس، آرامش، انرژی، شور، جوانی، خلاقیت، ظراقت، تمرکز، تفکر، نقد، الهام، دانش، هیجان، تعامل
- توضیح کاربرد (اختیاری)

#### 4. **مدیریت وضعیت**
- **فعال/غیرفعال** (`isActive`): کنترل دسترسی به دسته
- **نمایش/مخفی** (`isVisible`): کنترل نمایش در لیست‌ها
- **ترتیب نمایش** (`order`): مرتب‌سازی دسته‌ها

#### 5. **متادیتا**
- تعداد رویدادهای هر دسته
- تعداد بازدید
- تگ‌ها (برای جستجوی پیشرفته)

#### 6. **UI درختی (Tree View)**
- نمایش سلسله مراتبی
- Expand/Collapse زیردسته‌ها
- نمایش سطح هر دسته
- عملیات سریع (افزودن زیردسته، ویرایش، حذف)

---

## ساختار دیتابیس

### مدل: `TopicCategory`

```javascript
{
  // اطلاعات اصلی
  title: String,              // عنوان (required)
  slug: String,               // slug یکتا (auto-generated)
  description: String,        // توضیح (optional)

  // سلسله مراتب
  parentId: ObjectId,         // والد (null برای سطح اول)
  level: Number,              // سطح (auto-calculated)
  order: Number,              // ترتیب نمایش (default: 0)

  // ظاهر
  icon: String,               // emoji (default: '📁')
  baseColor: String,          // HEX color (default: '#F4A325')
  gradient: {
    start: String,            // HEX color
    end: String,              // HEX color
    direction: String,        // 'to-right', 'to-left', etc.
  },

  // حس و کاربرد
  mood: String,               // enum (default: 'خلاقیت')
  usage: String,              // توضیح کاربرد

  // وضعیت
  isActive: Boolean,          // فعال/غیرفعال (default: true)
  isVisible: Boolean,         // نمایش/مخفی (default: true)

  // متادیتا
  metadata: {
    eventsCount: Number,      // تعداد رویدادها
    viewCount: Number,        // تعداد بازدید
    tags: [String],           // تگ‌ها
  },

  // Audit
  createdBy: ObjectId,        // ایجادکننده
  updatedBy: ObjectId,        // آخرین ویرایش‌کننده
  createdAt: Date,
  updatedAt: Date,
}
```

### Virtual Fields
- `children`: لیست زیردسته‌ها (auto-populated)

### Indexes
```javascript
slug: 1            // یکتا
parentId: 1        // برای query کارآمد
level: 1           // مرتب‌سازی
[isActive, isVisible]: 1  // فیلتر
createdAt: -1      // مرتب‌سازی زمانی
```

---

## API Endpoints

### 1. لیست دسته‌بندی‌ها
```http
GET /api/dashboard/cat_topic
```

**Query Parameters:**
- `view`: `tree` | `flat` (default: `tree`)
- `parentId`: فیلتر براساس parent (`root` برای سطح اول)
- `level`: فیلتر براساس سطح
- `isActive`: فیلتر براساس وضعیت فعال/غیرفعال
- `isVisible`: فیلتر براساس نمایش/مخفی
- `search`: جستجو در عنوان، توضیح و slug

**Response (Tree View):**
```json
{
  "success": true,
  "data": [
    {
      "_id": "...",
      "title": "فرهنگی و هنری",
      "slug": "cultural-artistic",
      "icon": "🎭",
      "baseColor": "#F4A325",
      "level": 1,
      "children": [
        {
          "_id": "...",
          "title": "موسیقی",
          "parentId": "...",
          "level": 2,
          "children": []
        }
      ]
    }
  ],
  "count": 10,
  "view": "tree"
}
```

---

### 2. ایجاد دسته‌بندی جدید
```http
POST /api/dashboard/cat_topic
```

**Body:**
```json
{
  "title": "موسیقی",
  "description": "کنسرت، اجرا، موسیقی سنتی و مدرن",
  "parentId": "65f1a2b3c4d5e6f7g8h9i0j1",  // optional
  "icon": "🎵",
  "baseColor": "#10B981",
  "gradient": {
    "start": "#10B981",
    "end": "#059669",
    "direction": "to-right"
  },
  "mood": "هیجان",
  "usage": "کنسرت‌ها و اجراهای موسیقی",
  "isActive": true,
  "isVisible": true,
  "order": 0,
  "tags": ["موسیقی", "کنسرت", "اجرا"]
}
```

**Response:**
```json
{
  "success": true,
  "message": "دسته‌بندی با موفقیت ایجاد شد",
  "data": { /* دسته‌بندی ایجاد شده */ }
}
```

---

### 3. دریافت یک دسته‌بندی
```http
GET /api/dashboard/cat_topic/:id
```

**Response:**
```json
{
  "success": true,
  "data": {
    /* دسته‌بندی */
    "children": [ /* زیردسته‌ها */ ],
    "path": [ /* مسیر کامل (breadcrumb) */ ]
  }
}
```

---

### 4. ویرایش دسته‌بندی
```http
PUT /api/dashboard/cat_topic/:id
```

**Body:** (همه فیلدها اختیاری)
```json
{
  "title": "موسیقی کلاسیک",
  "isActive": false
}
```

**محدودیت‌ها:**
- نمی‌توان دسته را به خودش متصل کرد
- نمی‌توان دسته را به یکی از زیردسته‌هایش متصل کرد (جلوگیری از حلقه)

---

### 5. حذف دسته‌بندی
```http
DELETE /api/dashboard/cat_topic/:id
```

**محدودیت:**
- دسته‌هایی که زیردسته دارند قابل حذف نیستند
- ابتدا باید زیردسته‌ها حذف شوند

---

## رابط کاربری

### صفحه اصلی (`/dashboard/cat_topic`)

#### عناصر UI:

##### 1. **Header**
- عنوان صفحه با آیکن
- دکمه "دسته‌بندی جدید"
- دکمه "آپلود Excel"

##### 2. **فیلترها و جستجو**
- جستجو در عنوان، توضیح و slug
- انتخاب view: درختی / صاف
- فیلتر وضعیت: همه / فعال / غیرفعال
- فیلتر نمایش: همه / نمایش / مخفی

##### 3. **نمایش درختی (Tree View)**
- هر دسته شامل:
  - دکمه toggle برای Expand/Collapse
  - آیکن و عنوان
  - سطح (level badge)
  - Badge برای غیرفعال/مخفی
  - نوار رنگی (baseColor) در سمت راست
  - متادیتا: mood, تعداد رویدادها
  - دکمه‌های عملیات:
    - ➕ افزودن زیردسته
    - ✏️ ویرایش
    - 🗑️ حذف

##### 4. **مودال ایجاد/ویرایش**
- فرم شامل:
  - عنوان (required)
  - توضیح (optional)
  - انتخاب parent (dropdown)
  - انتخاب آیکن (emoji picker با 48 آیکن)
  - رنگ پایه (color picker)
  - گرادیانت (optional):
    - رنگ شروع
    - رنگ پایان
    - جهت گرادیانت
    - پیش‌نمایش زنده
  - حس و کاربرد (dropdown + text)
  - ترتیب نمایش (number)
  - تگ‌ها (multi-input)
  - توگل‌های فعال/غیرفعال و نمایش/مخفی

---

## نحوه استفاده

### 1. ایجاد دسته‌بندی سطح اول

```javascript
// مثال: فرهنگی و هنری
{
  title: "فرهنگی و هنری",
  description: "رویدادهای فرهنگی، هنری و ادبی برای تبادل تجربه و خلاقیت",
  parentId: null,  // سطح اول
  icon: "🎭",
  baseColor: "#F4A325",
  gradient: {
    start: "#F4A325",
    end: "#F59E0B",
    direction: "to-right"
  },
  mood: "خلاقیت",
  usage: "رویدادهای فرهنگی و هنری",
  isActive: true,
  isVisible: true,
  order: 0
}
```

### 2. ایجاد زیردسته (سطح دوم)

```javascript
// مثال: موسیقی (زیر فرهنگی و هنری)
{
  title: "موسیقی",
  description: "کنسرت، اجرا، موسیقی سنتی، مدرن، محلی، کنسرت خیابانی",
  parentId: "65f1a2b3c4d5e6f7g8h9i0j1",  // ID فرهنگی و هنری
  icon: "🎵",
  baseColor: "#10B981",
  mood: "هیجان",
  usage: "کنسرت‌ها و اجراهای موسیقی",
  isActive: true,
  isVisible: true,
  order: 1
}
```

### 3. دریافت tree کامل

```javascript
const response = await fetch('/api/dashboard/cat_topic?view=tree', {
  credentials: 'include'
});
const data = await response.json();

// data.data شامل آرایه‌ای از دسته‌های سطح اول با children است
```

### 4. جستجو

```javascript
const response = await fetch('/api/dashboard/cat_topic?search=موسیقی', {
  credentials: 'include'
});
```

---

## آپلود از Excel

### فرمت فایل Excel

فایل Excel باید شامل ستون‌های زیر باشد:

| ستون | نوع | الزامی | توضیح |
|------|-----|--------|-------|
| `title` | Text | ✅ | عنوان دسته‌بندی |
| `description` | Text | ❌ | توضیح اختیاری |
| `parentTitle` | Text | ❌ | عنوان والد (خالی برای سطح اول) |
| `icon` | Text | ❌ | emoji (default: 📁) |
| `baseColor` | Text | ❌ | رنگ HEX (default: #F4A325) |
| `gradientStart` | Text | ❌ | رنگ شروع گرادیانت |
| `gradientEnd` | Text | ❌ | رنگ پایان گرادیانت |
| `gradientDirection` | Text | ❌ | جهت گرادیانت |
| `mood` | Text | ❌ | حس (default: خلاقیت) |
| `usage` | Text | ❌ | کاربرد |
| `isActive` | Text | ❌ | true/false (default: true) |
| `isVisible` | Text | ❌ | true/false (default: true) |
| `order` | Number | ❌ | ترتیب (default: 0) |

### نمونه Excel

```
| title | description | parentTitle | icon | baseColor | mood | isActive | isVisible | order |
|-------|-------------|-------------|------|-----------|------|----------|-----------|-------|
| فرهنگی و هنری | رویدادهای فرهنگی | | 🎭 | #F4A325 | خلاقیت | true | true | 0 |
| موسیقی | کنسرت و اجرا | فرهنگی و هنری | 🎵 | #10B981 | هیجان | true | true | 1 |
| سینما و فیلم | اکران، نقد، آموزش | فرهنگی و هنری | 🎬 | #3B82F6 | تفکر | true | true | 2 |
```

### نحوه آپلود

```javascript
const formData = new FormData();
formData.append('file', excelFile);

const response = await fetch('/api/dashboard/cat_topic/upload-excel', {
  method: 'POST',
  credentials: 'include',
  body: formData
});

const result = await response.json();
// result.success برابر با تعداد دسته‌بندی‌های موفق
// result.failed برابر با تعداد خطاها
// result.errors لیست خطاها
```

**⚠️ توجه:** این قابلیت در حال حاضر در حال توسعه است و نیاز به نصب package `xlsx` دارد.

---

## نمونه کدها

### Client-side: دریافت و نمایش دسته‌بندی‌ها

```javascript
'use client';
import { useState, useEffect } from 'react';

export default function CategorySelector() {
  const [categories, setCategories] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetchCategories();
  }, []);

  const fetchCategories = async () => {
    try {
      const response = await fetch('/api/dashboard/cat_topic?view=tree', {
        credentials: 'include'
      });
      const data = await response.json();
      if (data.success) {
        setCategories(data.data);
      }
    } catch (error) {
      console.error('Error:', error);
    } finally {
      setLoading(false);
    }
  };

  const renderCategory = (category) => (
    <div key={category._id} style={{ marginRight: `${category.level * 20}px` }}>
      <div>
        <span>{category.icon}</span>
        <span>{category.title}</span>
      </div>
      {category.children?.map(renderCategory)}
    </div>
  );

  return (
    <div>
      {loading ? (
        <p>در حال بارگذاری...</p>
      ) : (
        categories.map(renderCategory)
      )}
    </div>
  );
}
```

### Server-side: استفاده در API

```javascript
import TopicCategory from '@/lib/models/TopicCategory.model';

// دریافت دسته‌بندی‌های فعال
const activeCategories = await TopicCategory.find({ isActive: true })
  .sort({ level: 1, order: 1 })
  .lean();

// دریافت breadcrumb یک دسته
const category = await TopicCategory.findById(categoryId);
const path = await category.getFullPath();
```

---

## Best Practices

### 1. **ساختار سلسله مراتبی**
- حداکثر 3-4 سطح توصیه می‌شود
- نام‌گذاری واضح و کوتاه
- از تکرار نام در سطوح مختلف پرهیز کنید

### 2. **رنگ‌بندی**
- رنگ‌های متفاوت برای دسته‌های سطح اول
- رنگ‌های مشابه برای زیردسته‌ها
- از contrast مناسب اطمینان حاصل کنید

### 3. **Mood & Usage**
- mood را متناسب با نوع رویداد انتخاب کنید
- usage را برای کاربران نهایی واضح بنویسید

### 4. **Performance**
- برای دریافت tree کامل از view=tree استفاده کنید
- برای dropdown ها از view=flat استفاده کنید
- از cache استفاده کنید (دسته‌بندی‌ها کم تغییر می‌کنند)

---

## مشکلات رایج و راه‌حل

### خطا: "نمی‌توان دسته‌بندی با زیردسته حذف کرد"
**راه‌حل:** ابتدا تمام زیردسته‌ها را حذف کنید، سپس دسته اصلی را حذف کنید.

### خطا: "نمی‌توان دسته‌بندی را به یکی از زیردسته‌های خود متصل کرد"
**راه‌حل:** ساختار سلسله مراتبی نمی‌تواند حلقه داشته باشد. parent جدید را تغییر دهید.

### دسته‌بندی در منو نمایش داده نمی‌شود
**بررسی:**
1. `isActive` = `true`
2. `isVisible` = `true`
3. parent (اگر دارد) نیز active و visible باشد
4. دسترسی کاربر به منوی categories

---

## مراحل بعدی (Roadmap)

- [ ] پیاده‌سازی کامل Excel Upload/Download
- [ ] قابلیت Drag & Drop برای reorder
- [ ] نمایش تصویر برای هر دسته
- [ ] چند زبانه (multilingual)
- [ ] Import/Export JSON
- [ ] نسخه Public API برای دریافت دسته‌بندی‌ها

---

## پشتیبانی

برای سوالات و مشکلات:
1. مستندات API: `/docs/API.md`
2. مستندات RBAC: `/docs/RBAC.md`
3. Issue Tracker: GitHub Issues

---

**آخرین به‌روزرسانی:** 2025-01-11  
**نسخه:** 1.0.0




