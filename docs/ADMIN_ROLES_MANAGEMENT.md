# 🎛️ مدیریت نقش‌های کاربران در پنل ادمین

این مستند راهنمای استفاده از قابلیت مدیریت نقش‌های کاربران در پنل مدیریت PlusMeet است.

---

## 📋 فهرست

1. [دسترسی به قابلیت](#دسترسی-به-قابلیت)
2. [نقش‌های موجود](#نقش-های-موجود)
3. [نحوه استفاده](#نحوه-استفاده)
4. [قوانین و محدودیت‌ها](#قوانین-و-محدودیت-ها)
5. [نکات امنیتی](#نکات-امنیتی)

---

## 🔑 دسترسی به قابلیت

### پیش‌نیاز

- نقش **Admin** برای دسترسی به پنل مدیریت
- توکن معتبر JWT

### مسیر

```
/admin/users
```

در این صفحه، لیست تمام کاربران را مشاهده می‌کنید و می‌توانید نقش‌های آن‌ها را مدیریت کنید.

---

## 🎭 نقش‌های موجود

| نقش           | نام فارسی   | ایموجی | دسترسی                        |
| ------------- | ----------- | ------ | ----------------------------- |
| `guest`       | مهمان       | 👤     | فقط مشاهده (غیرقابل تخصیص)    |
| `user`        | کاربر       | 👥     | تعامل اجتماعی، شرکت در رویداد |
| `event_owner` | مالک رویداد | ⭐     | ایجاد و مدیریت رویداد         |
| `moderator`   | ناظر        | 🛡️     | نظارت بر محتوا و گزارش‌ها     |
| `admin`       | مدیر        | 👑     | دسترسی کامل به سیستم          |

---

## 📝 نحوه استفاده

### مرحله 1: باز کردن Modal مدیریت نقش‌ها

1. وارد پنل ادمین شوید: `/admin/users`
2. کاربر مورد نظر را پیدا کنید
3. روی دکمه **مدیریت نقش‌ها** (آیکون گروه کاربران) کلیک کنید

![دکمه مدیریت نقش‌ها](../assets/screenshots/roles-button.png)

---

### مرحله 2: انتخاب نقش‌ها

یک Modal باز می‌شود که شامل:

- **اطلاعات کاربر**: آواتار، نام و شماره موبایل
- **لیست نقش‌های موجود**: تمام نقش‌های قابل تخصیص
- **Checkbox**: برای انتخاب/عدم انتخاب نقش

#### نحوه کار:

```
✅ Checked   = کاربر این نقش را دارد
☐ Unchecked = کاربر این نقش را ندارد
```

#### مثال:

```
☑️ کاربر
☑️ مالک رویداد
☐ ناظر
☐ مدیر
```

در این مثال، کاربر هم `user` است و هم `event_owner`.

---

### مرحله 3: ذخیره تغییرات

1. نقش‌های مورد نظر را انتخاب/حذف کنید
2. روی **ذخیره تغییرات** کلیک کنید
3. اگر موفقیت‌آمیز باشد، Modal بسته می‌شود و لیست کاربران به‌روزرسانی می‌شود

---

## 🎬 مثال‌های کاربردی

### مثال 1: ارتقا کاربر به Event Owner

**سناریو:** کاربر معمولی می‌خواهد رویداد بسازد.

```
قبل:
  نقش‌ها: [user]

بعد (در Admin Panel):
  ✅ کاربر
  ✅ مالک رویداد  👈 اضافه شد

نتیجه:
  نقش‌ها: [user, event_owner]
```

---

### مثال 2: تخصیص نقش Moderator

**سناریو:** می‌خواهیم کاربری را به عنوان ناظر محتوا منصوب کنیم.

```
قبل:
  نقش‌ها: [user]

بعد:
  ✅ کاربر
  ✅ ناظر  👈 اضافه شد

نتیجه:
  نقش‌ها: [user, moderator]
```

---

### مثال 3: حذف نقش Event Owner

**سناریو:** کاربر دیگر نمی‌خواهد رویداد بسازد.

```
قبل:
  نقش‌ها: [user, event_owner]

بعد:
  ✅ کاربر
  ☐ مالک رویداد  👈 حذف شد

نتیجه:
  نقش‌ها: [user]
```

---

## 🔒 قوانین و محدودیت‌ها

### 1. نقش پیش‌فرض

- **همه کاربران حداقل یک نقش باید داشته باشند**
- نقش `user` معمولاً همیشه باید باشد

### 2. نقش Admin

⚠️ **توجه:** تخصیص نقش `admin` به کاربران بسیار حساس است!

**توصیه:**

- فقط به افراد مورد اعتماد نقش Admin بدهید
- Admin می‌تواند:
  - همه کاربران را مدیریت کند
  - تنظیمات را تغییر دهد
  - دسترسی کامل به سیستم دارد

### 3. نقش Guest

- نقش `guest` **غیرقابل تخصیص** است
- فقط برای کاربران ناشناس (بدون حساب کاربری)

### 4. نقش‌های چندگانه

✅ **مجاز:** یک کاربر می‌تواند چند نقش داشته باشد

```javascript
roles: ["user", "event_owner", "moderator"]; // ✅ معتبر
```

### 5. حذف همه نقش‌ها

❌ **غیرمجاز:** نمی‌توانید همه نقش‌های کاربر را حذف کنید

```
اگر تمام نقش‌ها را uncheck کنید:
→ خطا: "کاربر باید حداقل یک نقش داشته باشد"
```

---

## 🛡️ نکات امنیتی

### 1. Authentication

- فقط کاربران با نقش `admin` می‌توانند نقش‌ها را تغییر دهند
- توکن JWT برای احراز هویت استفاده می‌شود

### 2. Authorization

API endpoint:

```http
PUT /api/admin/users/:id/roles
Authorization: Bearer {accessToken}
```

### 3. Activity Logging

تمام تغییرات نقش در ActivityLog ثبت می‌شود:

```javascript
{
  action: "role_update",
  userId: "09123456789",
  changedBy: "admin_phone_number",
  metadata: {
    oldRoles: ["user"],
    newRoles: ["user", "event_owner"],
    addedRoles: ["event_owner"],
    removedRoles: []
  },
  timestamp: "2025-01-27T..."
}
```

### 4. Rate Limiting

- محدودیت تعداد درخواست برای جلوگیری از سوء استفاده
- Max 20 بار تغییر نقش در هر دقیقه (توسط یک Admin)

---

## 💻 API Reference

### Endpoint

```http
PUT /api/admin/users/:id/roles
```

### Request Body

```json
{
  "roles": ["user", "event_owner"]
}
```

### Response (موفق)

```json
{
  "success": true,
  "message": "نقش‌های کاربر با موفقیت به‌روزرسانی شد",
  "data": {
    "user": {
      "_id": "...",
      "phoneNumber": "09123456789",
      "roles": ["user", "event_owner"],
      "updatedAt": "2025-01-27T..."
    }
  }
}
```

### Response (خطا)

```json
{
  "success": false,
  "error": "Validation Error",
  "message": "نقش‌ها نامعتبر است"
}
```

---

## 🧪 تست

### تست دستی

1. **باز کردن Modal:**

   ```
   /admin/users → کلیک روی دکمه نقش‌ها
   ```

2. **تغییر نقش:**

   ```
   ✅ User
   ✅ Event Owner
   → ذخیره
   ```

3. **بررسی نتیجه:**
   ```bash
   npm run check-user 09123456789
   ```

### تست با curl

```bash
curl -X PUT http://localhost:3000/api/admin/users/USER_ID/roles \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"roles": ["user", "event_owner"]}'
```

---

## 🎯 UI/UX Features

### 1. Loading State

```
[در حال بارگذاری نقش‌ها...]
  🔄 Spinner
```

### 2. Error Handling

```
❌ خطا در ذخیره تغییرات
   [تلاش مجدد]
```

### 3. Success Feedback

```
✅ نقش‌های کاربر با موفقیت به‌روزرسانی شد
   (Modal بسته می‌شود و لیست refresh می‌شود)
```

### 4. Responsive Design

- **Desktop:** Modal با عرض 600px در وسط صفحه
- **Mobile:** Modal تمام صفحه، از پایین می‌آید

---

## 🔍 عیب‌یابی

### مشکل 1: دکمه "مدیریت نقش‌ها" نمایش داده نمی‌شود

**علت:**

- کاربر Admin نیست

**راه‌حل:**

```bash
# بررسی نقش کاربر
npm run check-user 09123456789

# اضافه کردن نقش Admin (از MongoDB)
db.users.updateOne(
  { phoneNumber: "09123456789" },
  { $addToSet: { roles: "admin" } }
)
```

---

### مشکل 2: خطای 403 Forbidden

**علت:**

- توکن منقضی شده
- کاربر Admin نیست

**راه‌حل:**

1. Logout و Login مجدد
2. بررسی نقش‌های کاربر

---

### مشکل 3: Modal باز نمی‌شود

**علت:**

- خطا در fetch نقش‌های موجود

**راه‌حل:**

```javascript
// چک کردن Console برای خطا
console.error("Error fetching roles");

// بررسی API endpoint
GET / api / admin / roles;
```

---

### مشکل 4: لیست نقش‌ها در Modal نمایش داده نمی‌شود

**علائم:**

- Modal باز می‌شود اما لیست خالی است
- پیام "هیچ نقشی یافت نشد" نمایش داده می‌شود
- یا همیشه در حالت "در حال بارگذاری..." می‌ماند

**علت:**

1. API endpoint `/api/admin/roles` به درستی پاسخ نمی‌دهد
2. دیتابیس خالی است و نقش‌های استاتیک بارگذاری نمی‌شوند
3. خطای Network یا Authentication

**راه‌حل:**

```javascript
// 1. چک کردن Browser Console (F12)
console.log("✅ Roles fetched:", data);
console.log("📋 Assignable roles:", assignableRoles);

// 2. چک کردن Network Tab
// باید ببینید:
// GET /api/admin/roles → Status: 200
// Response: { success: true, data: [...] }

// 3. تست مستقیم API
// در Browser Console:
fetch("/api/admin/roles", {
  headers: {
    Authorization: `Bearer ${localStorage.getItem("accessToken")}`,
  },
})
  .then((res) => res.json())
  .then((data) => console.log("API Response:", data));
```

**اگر API کار نمی‌کند:**

```bash
# چک کردن Server Logs در Terminal
# باید ببینید:
GET /api/admin/roles 200

# اگر خطا وجود دارد:
Database roles not available, using static roles
```

---

### مشکل 5: رنگ Header Modal شفاف است

**علت:**

- CSS برای background header تنظیم نشده

**راه‌حل:**

این مشکل در نسخه‌های جدید برطرف شده. اگر هنوز وجود دارد:

```css
/* در RolesModal.module.css */
.modalHeader {
  background: var(--color-bg-primary);
  border-top-left-radius: 16px;
  border-top-right-radius: 16px;
}
```

---

## 📊 آمار و گزارش

### Query تعداد کاربران با نقش خاص

```javascript
// تعداد Event Owners
db.users.countDocuments({
  roles: "event_owner",
});

// تعداد Moderators
db.users.countDocuments({
  roles: "moderator",
});
```

### Query تغییرات نقش

```javascript
// آخرین تغییرات نقش
db.activitylogs
  .find({
    action: "role_update",
  })
  .sort({ timestamp: -1 })
  .limit(10);
```

---

## 🔗 مستندات مرتبط

- [User System](./USER_SYSTEM.md)
- [RBAC Guide](./RBAC_GUIDE.md)
- [Role Selection](./ROLE_SELECTION.md)
- [Role Upgrade](./ROLE_UPGRADE.md)
- [Admin Panel](./ADMIN_PANEL.md)

---

## 📞 پشتیبانی

مشکل یا سوالی دارید؟

- 📧 Email: admin@plusmeet.ir
- 🐛 [گزارش باگ](https://github.com/plusmeet/issues)

---

**آخرین به‌روزرسانی:** 2025-01-27
