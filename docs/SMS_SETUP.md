# 📱 راهنمای راه‌اندازی SMS.ir

## 🎯 مراحل راه‌اندازی

### 1. ثبت‌نام در SMS.ir

1. به سایت [sms.ir](https://sms.ir) مراجعه کنید
2. ثبت‌نام کنید و حساب کاربری ایجاد کنید
3. شماره خط (Line Number) دریافت کنید

### 2. دریافت API Key

1. وارد پنل کاربری شوید
2. به بخش **تنظیمات** > **API Key** بروید
3. API Key خود را کپی کنید

### 3. ایجاد الگوی پیامک (Template)

برای ارسال OTP، باید یک الگو در پنل SMS.ir ایجاد کنید:

1. به بخش **الگوهای پیامک** بروید
2. روی **ایجاد الگو** کلیک کنید
3. محتوای الگو:

```
کد تایید PlusMeet:
{{CODE}}

این کد تا 2 دقیقه اعتبار دارد.
```

4. بعد از تایید، **شناسه الگو (Template ID)** را یادداشت کنید

### 4. پیکربندی پروژه

فایل `.env` را ایجاد کنید (بر اساس `.env.example`):

```env
# SMS.ir Configuration
SMS_IR_API_KEY=your-api-key-here
SMS_IR_LINE_NUMBER=30007732999999
SMS_IR_TEMPLATE_ID=123456
```

---

## 📋 مثال‌های استفاده

### ارسال OTP

```javascript
import { sendOTP, generateOTP } from "@/lib/services/sms.service";

// تولید کد
const code = generateOTP(); // مثلاً: "12345"

// ارسال
const result = await sendOTP("09123456789", code);

if (result.success) {
  console.log("پیامک ارسال شد", result.messageId);
}
```

### ارسال پیامک ساده

```javascript
import { sendSMS } from "@/lib/services/sms.service";

const result = await sendSMS("09123456789", "سلام! به PlusMeet خوش آمدید.");
```

---

## 🧪 تست در محیط Development

در حالت development، کد OTP در console نمایش داده می‌شود:

```
🔐 OTP Code for 09123456789: 12345
```

این باعث می‌شود بدون نیاز به ارسال واقعی پیامک، بتوانید تست کنید.

---

## 📊 محدودیت‌های ارسال (Rate Limiting)

برای جلوگیری از سوءاستفاده:

- **یک OTP در 60 ثانیه** برای هر شماره
- **حداکثر 3 تلاش** برای تایید OTP
- **اعتبار 2 دقیقه** برای هر کد

---

## 💰 هزینه‌ها

| نوع پیامک            | هزینه تقریبی  |
| -------------------- | ------------- |
| پیامک معمولی         | 200-300 تومان |
| پیامک با الگو (OTP)  | 150-250 تومان |
| بسته اشتراکی (تخفیف) | بسته به حجم   |

---

## 🔧 عیب‌یابی

### خطا: "Invalid API Key"

- API Key را دوباره چک کنید
- مطمئن شوید که فاصله اضافی ندارد

### خطا: "Template not found"

- شناسه الگو را بررسی کنید
- مطمئن شوید الگو تایید شده است

### خطا: "Insufficient credit"

- موجودی حساب را چک کنید
- حساب خود را شارژ کنید

### پیامک ارسال نمی‌شود

- شماره خط را بررسی کنید
- وضعیت خط در پنل را چک کنید

---

## 📚 منابع بیشتر

- [مستندات API رسمی SMS.ir](https://sms.ir/api)
- [پنل کاربری SMS.ir](https://panel.sms.ir)
- [پشتیبانی: support@sms.ir](mailto:support@sms.ir)

---

## 🔐 امنیت

### نکات امنیتی مهم:

1. **هیچ‌وقت API Key را commit نکنید**

   - از `.env` استفاده کنید
   - `.env` را در `.gitignore` اضافه کنید

2. **Rate Limiting را فعال نگه دارید**

   - جلوگیری از حملات brute-force
   - محدود کردن تعداد درخواست‌ها

3. **IP Whitelist**

   - در پنل SMS.ir فقط IP سرور را مجاز کنید

4. **لاگ کردن**
   - تمام ارسال‌ها را لاگ کنید
   - فعالیت‌های مشکوک را رصد کنید

---

**آخرین به‌روزرسانی**: 27 اکتبر 2025




