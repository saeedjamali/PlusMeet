# 📋 مستندات مدیریت منوها

> سیستم جامع مدیریت منوهای چند سطحی در PlusMeet

## 📍 مسیر

```
/admin/settings/menus
```

## 🎯 امکانات

### 1️⃣ نمایش منوها

- **ساختار درختی (Tree View)**: نمایش منوها و زیرمنوها به صورت سلسله مراتبی
- **چند سطحی**: پشتیبانی از تعداد نامحدود سطوح (منو، زیرمنو، زیر-زیرمنو، ...)
- **اطلاعات کامل**: نمایش آیکون، عنوان، مسیر، و وضعیت هر منو

### 2️⃣ افزودن منو

**فیلدها**:

- `menuId` (الزامی): شناسه یکتا (مثال: `users.list`)
- `title` (الزامی): عنوان فارسی (مثال: "لیست کاربران")
- `titleEn`: عنوان انگلیسی (مثال: "User List")
- `path`: مسیر صفحه (مثال: `/admin/users`)
- `parentId`: شناسه منوی والد (برای زیرمنوها)
- `icon`: آیکون emoji (مثال: 👥)
- `order`: ترتیب نمایش (عدد)
- `isActive`: فعال/غیرفعال (checkbox)

**نکات**:

- اگر `path` خالی باشد، منو به عنوان گروه (folder) عمل می‌کند
- `menuId` باید یکتا باشد
- برای زیرمنو، `parentId` را از منوی والد انتخاب کنید

### 3️⃣ ویرایش منو

- کلیک روی آیکون ✏️ (ویرایش)
- تمام فیلدها قابل ویرایش هستند **به جز** `menuId`
- تغییرات بلافاصله اعمال می‌شود

### 4️⃣ حذف منو

- کلیک روی آیکون 🗑️ (حذف)
- **محدودیت**: نمی‌توانید منویی که دارای زیرمنو است را حذف کنید
- ابتدا باید زیرمنوها را حذف یا جابجا کنید

### 5️⃣ جانمایی مجدد منوها ✨

#### دکمه‌های جابجایی:

```
┌─────────────────────────┐
│  ↑  ↓  ←  →   ✏️  🗑️   │
│  └─┬─┘  └─┬─┘           │
│    │      │              │
│  Order  Level           │
└─────────────────────────┘
```

#### **↑ انتقال به بالا (Move Up)**

- جابجایی منو با منوی بالایی
- فقط در بین منوهای همسطح
- مثال:
  ```
  قبل:           بعد از کلیک ↑ روی "کاربران":
  داشبورد       کاربران
  کاربران   →   داشبورد
  رویدادها      رویدادها
  ```

#### **↓ انتقال به پایین (Move Down)**

- جابجایی منو با منوی پایینی
- فقط در بین منوهای همسطح
- مثال:
  ```
  قبل:           بعد از کلیک ↓ روی "داشبورد":
  داشبورد       کاربران
  کاربران   →   داشبورد
  رویدادها      رویدادها
  ```

#### **→ افزایش سطح (Indent / Make Submenu)**

- تبدیل منو به زیرمنوی منوی بالایی
- منوی بالایی به عنوان والد انتخاب می‌شود
- مثال:
  ```
  قبل:           بعد از کلیک → روی "لیست کاربران":
  کاربران       کاربران
  لیست کاربران    └── لیست کاربران  ← زیرمنو شد
  ایجاد کاربر     ایجاد کاربر
  ```

#### **← کاهش سطح (Outdent / Promote to Parent Level)**

- تبدیل زیرمنو به منوی همسطح با والدش
- اگر در سطح دوم باشد، به سطح اصلی منتقل می‌شود
- مثال:
  ```
  قبل:           بعد از کلیک ← روی "لیست کاربران":
  کاربران       کاربران
    └── لیست کاربران   لیست کاربران  ← به سطح اصلی آمد
    └── ایجاد کاربر       └── ایجاد کاربر
  ```

---

## 🌳 مثال ساختار چند سطحی

```
📊 داشبورد
👥 کاربران
   └── لیست کاربران (/admin/users)
   └── ایجاد کاربر (/admin/users/create)
📅 رویدادها
   └── لیست رویدادها (/admin/events)
   └── ایجاد رویداد (/admin/events/create)
🔐 مدیریت دسترسی
   └── نقش‌ها (/admin/rbac/roles)
⚙️ تنظیمات سیستم
   └── مدیریت منوها (/admin/settings/menus)
   └── همگام‌سازی API (/admin/sync-apis)
```

---

## 🎨 UI/UX

### دکمه‌های جابجایی (Move Controls)

- **رنگ**: خاکستری پیش‌فرض، آبی در hover
- **گروه‌بندی**: دکمه‌های جابجایی در یک بلوک جداگانه
- **Tooltip**: توضیح عملکرد هر دکمه در hover
- **Responsive**: در موبایل، دکمه‌ها به صورت عمودی نمایش داده می‌شوند

### دکمه‌های ویرایش/حذف (Edit Controls)

- **ویرایش**: آیکون قلم، رنگ آبی
- **حذف**: آیکون سطل زباله، رنگ قرمز
- **جداسازی**: این دکمه‌ها از دکمه‌های جابجایی جدا هستند

---

## 📝 نکات مهم

### ✅ انجام دهید:

- از `menuId` معنادار استفاده کنید (مثال: `users.list`, `events.create`)
- آیکون emoji مناسب برای هر منو انتخاب کنید
- `order` را به صورت 10, 20, 30, ... تنظیم کنید (برای فضای مناورت)
- قبل از حذف منو، زیرمنوها را بررسی کنید

### ❌ انجام ندهید:

- از `menuId` تکراری استفاده نکنید
- منوی دارای زیرمنو را حذف نکنید (ابتدا زیرمنوها را جابجا کنید)
- `path` منوی گروه (folder) را پر نکنید
- `order` منوهای مختلف را یکسان نکنید

---

## 🔧 استفاده پیشرفته

### ایجاد منوی چند سطحی:

1. **ایجاد منوی والد (Level 1)**:

   ```
   menuId: settings
   title: تنظیمات
   path: (خالی)
   parentId: (خالی)
   icon: ⚙️
   ```

2. **ایجاد زیرمنو (Level 2)**:

   ```
   menuId: settings.general
   title: تنظیمات عمومی
   path: /admin/settings/general
   parentId: settings
   ```

3. **ایجاد زیر-زیرمنو (Level 3)**:
   ```
   menuId: settings.general.profile
   title: تنظیمات پروفایل
   path: /admin/settings/general/profile
   parentId: settings.general
   ```

### تغییر ترتیب با دکمه‌ها:

```
وضعیت فعلی:
1. داشبورد (order: 1)
2. کاربران (order: 2)
3. رویدادها (order: 3)

اگر بخواهیم "رویدادها" را بالاتر ببریم:
1. کلیک ↑ روی "رویدادها" → جابجایی با "کاربران"
2. کلیک ↑ روی "رویدادها" → جابجایی با "داشبورد"

نتیجه:
1. رویدادها (order: 1)
2. داشبورد (order: 2)
3. کاربران (order: 3)
```

---

## 🚀 نکات بهینه‌سازی

- **کش کردن**: منوها در frontend کش می‌شوند، برای مشاهده تغییرات صفحه را رفرش کنید
- **Real-time**: تغییرات بلافاصله در sidebar نمایش داده می‌شوند
- **Undo**: در حال حاضر پشتیبانی نمی‌شود، با دقت تغییرات را اعمال کنید
- **Activity Log**: تمام تغییرات در activity log ثبت می‌شوند

---

## 🐛 عیب‌یابی

### منو در sidebar نمایش داده نمی‌شود:

1. بررسی کنید `isActive` فعال باشد
2. بررسی کنید نقش شما به این منو دسترسی داشته باشد
3. صفحه را رفرش کنید (Ctrl+F5)

### نمی‌توانم منو را حذف کنم:

- اگر منو دارای زیرمنو است، ابتدا زیرمنوها را حذف یا جابجا کنید

### دکمه‌های جابجایی کار نمی‌کنند:

- بررسی کنید console browser را برای خطاها
- اگر "در بالاترین/پایین‌ترین موقعیت" نمایش داده شد، عملیات امکان‌پذیر نیست

---

**نسخه**: 1.0.0  
**آخرین به‌روزرسانی**: 2025-10-29  
**نویسنده**: PlusMeet Team





