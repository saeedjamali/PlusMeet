# 📊 راهنمای آپلود دسته‌بندی‌ها از Excel

## 📋 فهرست
- [مقدمه](#مقدمه)
- [دانلود Template](#دانلود-template)
- [فرمت فایل Excel](#فرمت-فایل-excel)
- [نحوه استفاده](#نحوه-استفاده)
- [مثال‌های عملی](#مثال‌های-عملی)
- [خطاهای رایج](#خطاهای-رایج)

---

## مقدمه

قابلیت آپلود Excel به شما امکان می‌دهد دسته‌بندی‌های خود را به صورت دسته‌جمعی (bulk) وارد سیستم کنید.

### ✅ مزایا:
- ایجاد سریع چندین دسته‌بندی
- ورود داده‌های از پیش آماده
- امکان ویرایش آسان در Excel
- پشتیبانی از سلسله مراتب (parent-child)

---

## دانلود Template

### روش 1: از رابط کاربری
1. به صفحه `/dashboard/cat_topic` بروید
2. روی دکمه **"آپلود Excel"** کلیک کنید
3. در مودال باز شده، روی **"دانلود فایل نمونه (Template)"** کلیک کنید

### روش 2: مستقیم از API
```bash
curl -o template.xlsx http://localhost:3000/api/dashboard/cat_topic/upload-excel \
  -H "Cookie: your-auth-cookie"
```

---

## فرمت فایل Excel

### ستون‌های فایل (15 ستون):

| ردیف | نام ستون | نوع | الزامی | توضیحات | مثال |
|------|----------|-----|---------|---------|------|
| 1 | `title` | Text | ✅ | عنوان دسته‌بندی | `فرهنگی و هنری` |
| 2 | `code` | Text | ❌ | کد یکتا (انگلیسی، uppercase) | `CULTURE_ART` |
| 3 | `description` | Text | ❌ | توضیح مختصر | `رویدادهای فرهنگی، هنری و ادبی` |
| 4 | `parentTitle` | Text | ❌ | عنوان دسته والد (خالی = سطح اول) | `فرهنگی و هنری` |
| 5 | `icon` | Emoji | ❌ | آیکن (emoji) | `🎭` |
| 6 | `baseColor` | HEX | ❌ | رنگ پایه | `#F4A325` |
| 7 | `gradientStart` | HEX | ❌ | رنگ شروع گرادیانت | `#F4A325` |
| 8 | `gradientEnd` | HEX | ❌ | رنگ پایان گرادیانت | `#F59E0B` |
| 9 | `gradientDirection` | Text | ❌ | جهت گرادیانت | `to-right` |
| 10 | `mood` | Text | ❌ | حس و کاربرد | `خلاقیت` |
| 11 | `usage` | Text | ❌ | توضیح کاربرد | `رویدادهای فرهنگی و هنری` |
| 12 | `isActive` | Boolean | ❌ | فعال/غیرفعال | `true` یا `false` |
| 13 | `isVisible` | Boolean | ❌ | نمایش/مخفی | `true` یا `false` |
| 14 | `order` | Number | ❌ | ترتیب نمایش | `0`, `1`, `2`, ... |
| 15 | `tags` | Text | ❌ | تگ‌ها (جدا شده با کاما) | `فرهنگی,هنری,ادبی` |

---

### مقادیر پیش‌فرض:

اگر فیلدی خالی باشد، مقادیر زیر اعمال می‌شود:

```javascript
{
  code: null,        // اگر خالی باشد، از title تولید می‌شود
  description: '',
  parentTitle: null,  // سطح اول
  icon: '📁',
  baseColor: '#F4A325',
  gradientStart: null,
  gradientEnd: null,
  gradientDirection: 'to-right',
  mood: 'خلاقیت',
  usage: '',
  isActive: true,
  isVisible: true,
  order: 0,
  tags: []
}
```

### ⚠️ توجه مهم درباره `code`:
- اگر `code` خالی باشد، **خودکار** از `title` تولید می‌شود
- حروف فارسی به انگلیسی تبدیل می‌شوند (transliteration)
- مثال: `فرهنگی و هنری` → `FRHNGAY_V_HNRAY`
- اگر `code` وارد کنید، حتماً باید:
  - **یکتا** باشد (تکراری نباشد)
  - فقط شامل **حروف انگلیسی، اعداد، و underscore** باشد
  - **uppercase** استفاده شود (خودکار تبدیل می‌شود)

---

### گزینه‌های Mood:

یکی از این مقادیر را انتخاب کنید:
- `احساس`
- `آرامش`
- `انرژی`
- `شور`
- `جوانی`
- `خلاقیت` (پیش‌فرض)
- `ظراقت`
- `تمرکز`
- `تفکر`
- `نقد`
- `الهام`
- `دانش`
- `هیجان`
- `تعامل`

---

### جهت‌های Gradient:

اگر از گرادیانت استفاده می‌کنید:
- `to-right` (راست به چپ)
- `to-left` (چپ به راست)
- `to-top` (پایین به بالا)
- `to-bottom` (بالا به پایین)
- `to-top-right` (مورب راست-بالا)
- `to-bottom-right` (مورب راست-پایین)

---

## نحوه استفاده

### مرحله 1️⃣: دانلود Template
دکمه **"دانلود فایل نمونه"** را کلیک کنید.

### مرحله 2️⃣: ویرایش در Excel
فایل را در Excel باز کنید و داده‌های خود را وارد کنید.

**نکات مهم:**
- ردیف اول (header) را **حذف نکنید**
- نام ستون‌ها را **تغییر ندهید**
- از **RTL** برای فارسی استفاده کنید
- فایل را با فرمت `.xlsx` یا `.xls` ذخیره کنید

### مرحله 3️⃣: آپلود
1. در مودال، فایل را به Drop Zone بکشید یا کلیک کنید
2. دکمه **"آپلود و ایجاد"** را بزنید
3. منتظر نتیجه بمانید

### مرحله 4️⃣: بررسی نتیجه
پس از آپلود، نتیجه نمایش داده می‌شود:
- ✅ **موفق**: تعداد دسته‌بندی‌های ایجاد شده
- ❌ **ناموفق**: تعداد خطاها
- 📋 **لیست خطاها**: جزئیات هر خطا

---

## مثال‌های عملی

### مثال 1: دسته‌بندی ساده (سطح اول)

```excel
| title          | code         | description                  | parentTitle | icon | baseColor | mood     | isActive | isVisible | order |
|----------------|--------------|------------------------------|-------------|------|-----------|----------|----------|-----------|-------|
| فرهنگی و هنری | CULTURE_ART  | رویدادهای فرهنگی و هنری      |             | 🎭   | #F4A325   | خلاقیت   | true     | true      | 0     |
| ورزشی          | SPORTS       | رویدادهای ورزشی              |             | ⚽   | #10B981   | انرژی    | true     | true      | 1     |
| آموزشی         |              | دوره‌ها و کارگاه‌های آموزشی |             | 📚   | #3B82F6   | تمرکز    | true     | true      | 2     |
```

**💡 نکته:** ردیف سوم `code` ندارد، پس خودکار از عنوان تولید می‌شود → `AMVZSHAY`

---

### مثال 2: سلسله مراتب (Parent-Child)

```excel
| title             | code          | description                    | parentTitle      | icon | baseColor | mood    |
|-------------------|---------------|--------------------------------|------------------|------|-----------|---------|
| فرهنگی و هنری    | CULTURE_ART   | دسته اصلی                      |                  | 🎭   | #F4A325   | خلاقیت  |
| موسیقی            | MUSIC         | کنسرت و اجرا                   | فرهنگی و هنری   | 🎵   | #10B981   | هیجان   |
| کنسرت خیابانی     |               | اجراهای موسیقی در فضای باز    | موسیقی           | 🎤   | #059669   | انرژی   |
| سینما و فیلم      | CINEMA_FILM   | اکران و نقد                    | فرهنگی و هنری   | 🎬   | #3B82F6   | تفکر    |
```

**⚠️ نکته‌های مهم:** 
- والد باید **قبل از** فرزند در فایل وارد شود!
- "کنسرت خیابانی" code نداره → خودکار `KNSRT_KHYABANAY` تولید میشه

---

### مثال 3: با گرادیانت

```excel
| title       | code   | icon | baseColor | gradientStart | gradientEnd | gradientDirection | mood     |
|-------------|--------|------|-----------|---------------|-------------|-------------------|----------|
| موسیقی      | MUSIC  | 🎵   | #F4A325   | #F4A325       | #F59E0B     | to-right          | هیجان    |
| سینما       | CINEMA | 🎬   | #3B82F6   | #3B82F6       | #1E40AF     | to-bottom         | تفکر     |
| ورزش        | SPORT  | ⚽   | #10B981   | #10B981       | #059669     | to-top-right      | انرژی    |
```

---

### مثال 4: با تگ‌ها

```excel
| title          | code         | description        | tags                          |
|----------------|--------------|--------------------|------------------------------ ---|
| فرهنگی و هنری | CULTURE_ART  | رویدادهای فرهنگی  | فرهنگی,هنری,ادبی,تئاتر      |
| موسیقی         | MUSIC        | کنسرت و اجرا      | موسیقی,کنسرت,اجرا,سنتی       |
| ورزشی          |              | رویدادهای ورزشی   | ورزش,فوتبال,بسکتبال,والیبال |
```

**💡 نکته:** ردیف آخر code نداره → خودکار `VRZSHAY` تولید میشه

---

## خطاهای رایج

### ❌ خطا 1: "عنوان الزامی است"
**علت:** ستون `title` خالی است  
**راه‌حل:** عنوان را وارد کنید

---

### ❌ خطا 2: "والد یافت نشد"
**علت:** `parentTitle` به دسته‌ای اشاره می‌کند که هنوز ایجاد نشده  
**راه‌حل:** 
- والد را **قبل از** فرزند قرار دهید
- یا نام والد را صحیح وارد کنید (دقیقاً مطابق با `title` والد)

---

### ❌ خطا 3: "فایل Excel خالی است"
**علت:** فایل هیچ ردیفی ندارد یا فرمت اشتباه است  
**راه‌حل:**
- حداقل یک ردیف داده وارد کنید
- از template اصلی استفاده کنید

---

### ❌ خطا 4: "فقط فایل‌های Excel مجاز هستند"
**علت:** فرمت فایل `.xlsx` یا `.xls` نیست  
**راه‌حل:** فایل را با فرمت صحیح ذخیره کنید

---

### ❌ خطا 5: "حجم فایل نباید بیشتر از 5 مگابایت باشد"
**علت:** فایل خیلی بزرگ است  
**راه‌حل:**
- تعداد ردیف‌ها را کاهش دهید
- یا چند بار آپلود کنید

---

### ❌ خطا 6: "دسته‌بندی با این کد قبلاً ثبت شده است"
**علت:** فیلد `code` تکراری است  
**راه‌حل:**
- `code` را یکتا کنید (مثلاً: `MUSIC_1`, `MUSIC_2`)
- یا `code` را خالی بگذارید تا خودکار تولید شود
- توجه: اگر از عناوین فارسی یکسان استفاده می‌کنید، کدهای تولید شده خودکار ممکن است با یک `_1`, `_2`, ... متمایز شوند

---

## Best Practices

### ✅ انجام دهید:
- ابتدا template را دانلود کنید
- نام ستون‌ها را حفظ کنید
- والدها را قبل از فرزندان قرار دهید
- از رنگ‌های HEX معتبر استفاده کنید (`#RRGGBB`)
- اگر `code` دستی وارد می‌کنید، یکتا بودن آن را بررسی کنید
- برای تولید خودکار `code`، فیلد را خالی بگذارید
- قبل از آپلود، داده‌ها را بررسی کنید

### ❌ انجام ندهید:
- نام ستون‌ها را تغییر ندهید
- ردیف header را حذف نکنید
- از `code` تکراری استفاده نکنید
- فرزند را قبل از والد قرار ندهید
- از رنگ‌های نامعتبر استفاده نکنید
- فایل‌های بزرگتر از 5MB آپلود نکنید

---

## نکات تکمیلی

### 🔄 ترتیب پردازش
ردیف‌ها **به ترتیب** پردازش می‌شوند (از بالا به پایین).

### 🏗️ سلسله مراتب
می‌توانید تا **چندین سطح** عمق ایجاد کنید:
```
فرهنگی و هنری (سطح 1)
  └─ موسیقی (سطح 2)
      └─ کنسرت خیابانی (سطح 3)
          └─ جاز خیابانی (سطح 4)
```

### 📊 آمار
بعد از آپلود، آمار دقیق نمایش داده می‌شود:
- تعداد موفق
- تعداد ناموفق
- لیست کامل خطاها

### 🔁 Rollback
اگر خطایی رخ داد:
- دسته‌بندی‌های موفق **ذخیره می‌شوند**
- دسته‌بندی‌های ناموفق نادیده گرفته می‌شوند
- می‌توانید فقط ردیف‌های خطا را اصلاح و دوباره آپلود کنید

---

## پشتیبانی

### مشکل دارید؟
1. مستندات `/docs/TOPIC_CATEGORIES.md` را بخوانید
2. فایل template را مجدداً دانلود کنید
3. با تیم پشتیبانی تماس بگیرید

### گزارش باگ
اگر مشکلی پیدا کردید، موارد زیر را ارسال کنید:
- فایل Excel مشکل‌دار
- پیام خطا
- Screenshot از مودال آپلود

---

**آخرین به‌روزرسانی:** 2025-01-11  
**نسخه:** 1.0.0  
**Compatible با:** xlsx v0.18+

